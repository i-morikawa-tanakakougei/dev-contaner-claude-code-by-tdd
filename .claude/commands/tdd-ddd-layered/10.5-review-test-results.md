Use the 10.5-review-test-results subagent to review comprehensive test execution results and analyze quality metrics before refactoring. This command MUST USE the specialized 10.5-review-test-results subagent for optimal test results review.

## Metadata
- **Prerequisites**: All tests executed and results available (10-run-all-tests)
- **Input**: Issue number(s) (required), feature name (optional)
- **Output**: 
  - Test results analysis report
  - Quality metrics assessment
  - Updated `docs/use_cases/issue-X-Y.json` metadata with test analysis
  - Refactoring recommendations or required fixes
- **Dependencies**: Test execution results, coverage reports, implementation code
- **Execution Timing**: After test execution, before refactoring

## 🎯 **TDD/DDD/LAYERED PROCESS CONTEXT**

**🔄 Core Workflow**: Vision(00) → Structure(01) → Sprint(02) → Sprint Review(02.5) → Use-Case(03) → Domain(04) → Design Review(04.5) → Tests(05) → Test Review(05.5) → Domain(06) → App(07) → Infra(08) → UI(09) → Test(10) → **Test Results Review(10.5)** → Refactor(11) → Evolve(12) → Review(13) → Feedback(14) → PR(15) → Status(16)

**🎨 Architecture**: Clean Architecture (Domain→Application→Infrastructure→Presentation)  
**🧪 Development**: Test-Driven Development (RED→GREEN→REFACTOR)  
**🏗️ Design**: Domain-Driven Design (Entity, Value Object, Aggregate, Repository)  
**📋 Requirements**: Given-When-Then scenarios with complete traceability  
**🔄 Evolution**: Continuous scenario evolution via /evolve-scenarios command

> 📖 **Document Management System**: [README.md](./README.md)  
> 🗺️ **Current Position**: Sprint Execution Phase - Test Results Review (10.5/16)  
> 🎯 **Phase Purpose**: Analyze test results and quality metrics before refactoring  
> ⬅️ **Previous Stage**: 10-run-all-tests (Comprehensive Test Execution)  
> ➡️ **Next Stage**: 11-refactor (Refactoring)
>
> **📋 3-Layer Architecture Operations**:  
> - 🎯 **Strategic**: `docs/use_cases/core/index.md` (Validate against scenarios)  
> - 📊 **Tactical**: `docs/use_cases/index.md` (Update quality status)  
> - 🔧 **Execution**: `docs/use_cases/issue-X-Y.json` (Track test analysis results)

## 🔍 **TEST RESULTS REVIEW: ANALYSIS ONLY**

**⚠️ Important Notice:**
- **This step is TEST ANALYSIS ONLY** - Analyze and assess test execution results
- **NO IMPLEMENTATION CHANGES** - Focus on evaluation and quality assessment  
- **Quality analysis** - Review test coverage, performance metrics, and code quality
- **Generate analysis reports ONLY** - No code modifications

**Review Process:**
1. `10-run-all-tests` ← All tests executed and results generated
2. `10.5-review-test-results` ← **【YOU ARE HERE】Test results analysis and quality assessment**
3. `11-refactor` ← Refactoring begins (if approved)
4. Alternative: Fix critical issues ← Address blocking problems (if major issues found)

**Critical Test Analysis Focus:**
- ✅ Test coverage analysis and gap identification
- ✅ Performance bottlenecks and optimization opportunities
- ✅ Quality metrics assessment (complexity, maintainability)
- ✅ Architectural compliance validation

## Common Errors and Solutions

### ❌ Error Case 1: Test execution results not found
**Cause**: Test results review attempted before test execution  
**Solution**: 
```bash
# Execute tests first
/run-all-tests <issue-number>
# Then proceed with review
/review-test-results <issue-number>
```

### ❌ Error Case 2: Low test coverage detected
**Cause**: Implementation doesn't have adequate test coverage  
**Solution**: 
- Identify uncovered code paths and add tests
- Focus on critical business logic coverage
- Ensure Given-When-Then scenarios are fully tested

### ❌ Error Case 3: Performance issues detected
**Cause**: Implementation has performance bottlenecks  
**Solution**: 
- Analyze slow tests and optimize critical paths
- Review database queries and external dependencies
- Consider caching strategies for expensive operations

## Execution Examples

### ✅ Success Example
```bash
$ /review-test-results 3
🔍 Issues: #3 のテスト結果レビューを開始します
📊 テスト実行結果分析中...
  📈 テスト成功率: 100% (45/45テスト)
  📊 カバレッジ分析: 85% (target: 80%+)
  ⏱️ 実行時間: 2.3秒 (acceptable)
🎯 Given-When-Thenカバレッジ確認中...
  ✅ シナリオ実装率: 100% (5/5シナリオ)
  ✅ 受け入れ基準: すべて満たしている
📈 品質メトリクス評価中...
  ✅ 循環複雑度: 平均3.2 (good)
  ✅ 保守性指数: 82 (high)
  ✅ アーキテクチャ準拠: DDD/Clean違反なし
✅ テスト結果分析完了: リファクタリング推奨
🎉 テスト結果レビュー完了!
```

### ❌ Failure Example and Fix
```bash
$ /review-test-results 3
🔍 Issues: #3 のテスト結果レビューを開始します
❌ テスト結果に問題が検出されました:
  ⚠️ カバレッジ不足: 65% (target: 80%+)
  ⚠️ 性能問題: 実行時間15秒 (slow tests detected)
  ⚠️ 複雑度警告: 3個のメソッドで複雑度10+
💡 リファクタリング前に修正が必要です:
  1. カバレッジ向上: 未テスト箇所の特定と追加
  2. 性能改善: 遅いテストの最適化
  3. 複雑度削減: 高複雑度メソッドの分割

# Fix: Address issues before refactoring
$ # Add tests and optimize performance
$ /review-test-results 3  # Re-review
```

## 📋 **TEST RESULTS REVIEW TASK CHECKLIST**

**Use this checklist for comprehensive test results quality assessment:**

### 🔴 Required Tasks

#### **📊 Test Execution Results Analysis**
- [ ] **Verify test success rate**: Check overall test pass/fail ratio
- [ ] **Analyze failed tests**: Investigate any failing tests and root causes
- [ ] **Review test execution time**: Assess performance of test suite
- [ ] **Check test stability**: Verify tests run consistently without flaky failures

#### **📈 Coverage Analysis**
- [ ] **Generate coverage reports**: Execute coverage analysis tools
- [ ] **Analyze line coverage**: Review code coverage percentages by layer
- [ ] **Assess branch coverage**: Evaluate decision path coverage
- [ ] **Identify coverage gaps**: Find critical untested code paths

#### **🎯 Scenario Implementation Validation**
- [ ] **Map tests to scenarios**: Verify all Given-When-Then scenarios are implemented
- [ ] **Check acceptance criteria**: Confirm all acceptance criteria are validated
- [ ] **Review business rule testing**: Ensure business rules are thoroughly tested
- [ ] **Validate error scenario coverage**: Check error cases are properly tested

### 🟡 Recommended Tasks

#### **📈 Quality Metrics Assessment**
- [ ] **Analyze code complexity**: Review cyclomatic complexity metrics
- [ ] **Assess maintainability**: Evaluate maintainability index scores
- [ ] **Check code duplication**: Identify duplicated code that needs refactoring
- [ ] **Review architectural compliance**: Validate Clean Architecture and DDD principles

#### **⚡ Performance Analysis**
- [ ] **Identify slow tests**: Find tests with long execution times
- [ ] **Analyze bottlenecks**: Review performance profiling results
- [ ] **Check resource usage**: Assess memory and CPU usage patterns
- [ ] **Evaluate scalability**: Consider performance under load

### 🟢 Optional Tasks

#### **🔍 Advanced Quality Assessment**
- [ ] **Security vulnerability scan**: Check for potential security issues
- [ ] **Dependency analysis**: Review third-party dependencies and versions
- [ ] **Technical debt assessment**: Identify areas requiring technical debt reduction
- [ ] **Documentation coverage**: Verify public APIs have adequate documentation

#### **💰 Domain-Specific Analysis (例: 円建て対応)**
- [ ] **Financial calculation accuracy**: Verify precision in money calculations
- [ ] **Currency handling validation**: Check JPY-specific calculations are correct
- [ ] **Regulatory compliance check**: Ensure financial regulations are met
- [ ] **Edge case coverage**: Verify financial edge cases are tested

#### **🔄 Continuous Improvement Assessment**
- [ ] **Test suite evolution**: Evaluate how tests can evolve with requirements
- [ ] **Automation opportunities**: Identify areas for test automation improvement
- [ ] **Quality gate effectiveness**: Assess current quality gates and standards
- [ ] **Team productivity impact**: Consider impact on development velocity

### **📝 Documentation and Communication**
- [ ] **Create test analysis report**: Generate comprehensive test results assessment
- [ ] **Document quality metrics**: Record current quality baseline
- [ ] **List improvement opportunities**: Identify specific areas for enhancement
- [ ] **Generate refactoring roadmap**: Provide clear next steps for improvement

**💡 Pro Tip**: Focus on coverage gaps and performance bottlenecks - these often reveal design issues!

## 📊 **TEST RESULTS QUALITY GATES**

### **🟢 APPROVED - Ready for Refactoring**
- ✅ High test success rate (95%+ passing)
- ✅ Adequate test coverage (80%+ line coverage)
- ✅ Good performance metrics (reasonable execution time)
- ✅ Complete scenario implementation
- ✅ Acceptable quality metrics (complexity, maintainability)
- ✅ No critical architectural violations

### **🟡 CONDITIONAL APPROVAL - Minor Issues**
- ⚠️ Minor coverage gaps (70-80% coverage)
- ⚠️ Some performance optimization opportunities
- ⚠️ Quality improvements recommended
- ✅ Core functionality working correctly
- ✅ No blocking quality issues identified

### **🔴 REJECTED - Critical Issues Require Fixes**
- ❌ Low test success rate (< 95%)
- ❌ Inadequate test coverage (< 70%)
- ❌ Significant performance problems
- ❌ Missing scenario implementations
- ❌ High code complexity or maintainability issues
- 🔄 **Action**: Fix critical issues before refactoring

## Task Details

**🤖 Agent Integration**: This command MUST USE PROACTIVELY the specialized `10.5-review-test-results` subagent for optimal test results review. Claude Code should automatically delegate this task to the 10.5-review-test-results subagent based on the command description.

1. **Pre-execution Validation**:
   ```bash
   # 🔧 Load lightweight structure validation
   source "$(dirname "${BASH_SOURCE[0]}")/_validate_structure.sh"
   
   # Validate issue number requirement
   if [[ $# -eq 0 ]]; then
       echo "エラー: 最低1つのイシュー番号が必要です"
       echo "使用例:"
       echo "  /review-test-results 3        # 単一イシューのテスト結果レビュー"
       echo "  /review-test-results 3,15     # 複数イシューのテスト結果レビュー"
       exit 1
   fi
   
   # Parse issue numbers
   IFS=',' read -ra ISSUES <<< "$1"
   echo "🔍 Issues: $(printf '#%s ' "${ISSUES[@]}")のテスト結果レビューを開始します"
   
   # Execute test results structure validation
   if ! main "10.5-review-test-results"; then
       echo "❌ テスト結果構造確認が失敗しました"
       exit 1
   fi
   
   echo "✅ 前提条件確認完了: テスト結果レビュー準備完了"
   ```

2. **Context Preparation and Agent Execution**:
   ```bash
   # 🔄 Prepare context for agent (Pattern B: Hybrid approach)
   echo "🔍 コンテキスト準備とエージェント起動..."
   
   # Create context file with test results review information
   context_file="/workspace/.claude/context/current-command-context.json"
   current_time=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
   
   # Build context JSON for test results review
   cat > "$context_file" <<EOF
   {
     "command": "review-test-results",
     "timestamp": "$current_time",
     "issue_numbers": [$(IFS=,; printf '%s\n' "${ISSUES[@]}" | paste -sd,)],
     "phase": "test-results-review",
     "context": {
       "expected_outputs": [
         "docs/analysis/test-results-report.json",
         "docs/analysis/coverage-analysis.md",
         "docs/analysis/quality-metrics.md"
       ],
       "architecture_patterns": ["Test Coverage", "Quality Metrics", "Performance Analysis"]
     },
     "additional_instructions": "テスト実行結果の包括的分析を実行してください。カバレッジ評価、品質メトリクス分析、パフォーマンス評価を行い、リファクタリング準備状況を判定してください。",
     "special_considerations": [
       "テスト成功率とカバレッジの詳細分析",
       "Given-When-Thenシナリオ実装検証",
       "品質メトリクスとリファクタリング準備評価",
       "パフォーマンスボトルネックの特定"
     ],
     "custom_context": {
       "coverage_analysis": true,
       "quality_metrics": true,
       "performance_assessment": true,
       "refactoring_readiness": true
     }
   }
   EOF
   
   # Extract issue list and feature name
   issue_list=$(IFS=-; echo "${issue_numbers[*]}")
   if [[ ${#other_args[@]} -gt 0 ]]; then
       feature_name="${other_args[0]}"
   else
       # Feature name will be extracted from existing metadata files
       feature_name=""
   fi
   
   echo "🔍 Issues: $(printf '#%s ' "${issue_numbers[@]}")のテスト結果レビューを開始します"
   
   # 📊 Validate that test execution results exist
   echo "📊 前提条件確認中..."
   
   # Check if tests directory exists
   if [[ ! -d "tests/" ]]; then
       echo "❌ テストディレクトリが見つかりません"
       echo "💡 最初に以下のコマンドでテストを実行してください:"
       echo "   /run-all-tests ${issue_numbers[*]}"
       exit 1
   fi
   
   echo "✅ 前提条件確認完了: テスト実行結果の準備完了"
   ```

2. **Context Preparation and Agent Execution**:
   ```bash
   # 🔄 Prepare context for agent (Pattern B: Hybrid approach)
   echo "📊 コンテキスト準備とエージェント起動..."
   
   # Create context file with test results review information
   context_file="/workspace/.claude/context/current-command-context.json"
   current_time=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
   
   # Build context JSON for test results review
   cat > "$context_file" <<EOF
   {
     "command": "review-test-results",
     "timestamp": "$current_time",
     "issue_numbers": [$(IFS=,; echo "${issue_numbers[*]}")],
     "feature_name": "${feature_name:-"auto-detect"}",
     "phase": "test-results-review",
     "context": {
       "expected_outputs": [
         "docs/reviews/test-results-review-$(date +%Y%m%d).md",
         "docs/analysis/test-coverage-report.html",
         "docs/analysis/quality-metrics-report.json"
       ],
       "architecture_patterns": ["Test Analysis", "Quality Assessment", "Coverage Analysis"]
     },
     "additional_instructions": "テスト実行結果の包括的分析と品質評価を実行してください。テスト成功率、カバレッジ、パフォーマンス、シナリオ実装の完全性を評価し、リファクタリング準備状況の判定を行ってください。",
     "special_considerations": [
       "テスト成功率とスタビリティの詳細分析",
       "レイヤーアーキテクチャに対応したカバレッジ分析",
       "Given-When-Thenシナリオ実装の完全性確認",
       "品質メトリクス（複雑度・保守性・重複）の総合評価"
     ],
     "custom_context": {
       "test_execution_analysis": true,
       "coverage_assessment": true,
       "quality_metrics_evaluation": true,
       "scenario_validation": true
     }
   }
   EOF
   
   echo "✅ コンテキストファイル作成完了: $context_file"
   
   # 🤖 Execute the specialized agent with Task tool
   echo ""
   echo "📊 テスト結果レビューエージェントを起動します..."
   echo "専門エージェントがテスト品質と実装完成度を評価します"
   echo ""
   
   # Task tool execution with comprehensive prompt
   task_prompt="テスト結果レビュータスクを実行してください。

## コンテキスト情報の取得
1. 一時コンテキスト（イシュー情報）:
   - /workspace/.claude/context/current-command-context.json を読み込み

2. テスト実行状況の確認:
   - tests/ ディレクトリでテスト実行とカバレッジ分析
   - docs/test_results/ で既存のテスト結果を確認
   - pytest --cov でリアルタイムカバレッジ測定

## 実行タスク
1. テスト実行結果分析（成功率・失敗原因・安定性）
2. カバレッジ分析（ライン・ブランチ・レイヤー別）
3. シナリオ実装検証（Given-When-Then・受け入れ基準）
4. 品質メトリクス評価（複雑度・保守性・重複）
5. パフォーマンス分析（実行時間・ボトルネック）
6. アーキテクチャ準拠性確認（Clean Architecture/DDD）
7. 承認状況判定（APPROVED/CONDITIONAL_APPROVAL/REJECTED）
8. 包括的テスト分析レポート生成

## 重要: 標準化出力形式の遵守
レポートは必ず以下の構造化セクションで終了してください：

### 📊 実行サマリー
各Critical Taskの完了状態を✅/❌で明記

### 📋 総合判定
APPROVED/CONDITIONAL_APPROVAL/REJECTED のいずれかを明記

### 💡 次のステップ
判定に基づく具体的なアクションアイテムを列挙

## 処理完了後
- テスト結果分析レポートの作成
- 品質メトリクスとベースライン設定
- 次のステップ（リファクタリング・修正）への案内"

   # Execute with specialized 10.5-review-test-results subagent
   # The 10.5-review-test-results subagent will be automatically invoked based on the task description
   
   agent_exit_code=$?
   echo "✅ 専用エージェント実行完了 (終了コード: $agent_exit_code)"
   - Missing scenario implementations
   - High code complexity or maintainability issues
   - Action: Fix critical issues before refactoring
   
   OUTPUT REQUIREMENTS:
   - Generate comprehensive analysis report in docs/reviews/
   - Update metadata files with analysis status
   - Provide clear refactoring decision with next steps
   - Document specific issues and recommendations
   - Focus on coverage gaps and performance bottlenecks
   - Ensure quality gate validation for refactoring safety"
   
   # Execute the specialized agent
   claude_task="$task_context" \
       claude_agent="10.5-review-test-results" \
       claude_working_dir="$(pwd)" \
       claude_issues="$(printf '%s,' "${issue_numbers[@]}" | sed 's/,$//')" \
       claude_feature_name="$feature_name" \
       claude --agent
   
   agent_exit_code=$?
   echo "✅ 専用エージェント実行完了 (終了コード: $agent_exit_code)"
   ```

3. **Agent Result Verification**:
   ```bash
   # 🔧 Load advanced task verification library
   source "$(dirname "${BASH_SOURCE[0]}")/_task_verification.sh"
   
   # 🔍 Verify agent execution results with advanced task checking
   echo "🔍 エージェント結果検証中..."
   
   verification_issues=()
   
   # Check for analysis report generation
   analysis_report_pattern="docs/reviews/test-results-analysis-${issue_list}-*.md"
   analysis_reports=($(ls $analysis_report_pattern 2>/dev/null))
   
   if [[ ${#analysis_reports[@]} -eq 0 ]]; then
       verification_issues+=("テスト結果分析レポートが生成されていません")
       echo "❌ 分析レポートが見つかりません"
   else
       latest_report="${analysis_reports[-1]}"  # Get most recent
       echo "  ✅ 分析レポート: $latest_report"
       
       # ✨ New: Advanced task verification with retry capability
       echo "🔍 Critical tasks確認中..."
       if ! verify_critical_tasks "10.5-review-test-results" "$latest_report"; then
           echo "⚠️ Critical tasks確認で問題が検出されました - 再実行を試行します"
           
           # Prepare retry context with specific issues
           prepare_retry_context "1" "${verification_issues[@]}"
           
           # Execute agent with retry (implement agent call here)
           echo "🔄 エージェント再実行を準備中..."
           echo "💡 手動での再実行が必要です: /review-test-results ${issue_numbers[*]}"
           
           # For now, show the issues and continue
           echo "❌ 以下の問題が検出されました:"
           for issue in "${verification_issues[@]}"; do
               echo "  - $issue"
           done
       else
           echo "✅ Critical tasks確認完了"
       fi
       
       # Basic content verification (keeping existing checks)
       if ! grep -q "ステータス:" "$latest_report"; then
           verification_issues+=("分析レポートにステータスが記載されていません")
       fi
       
       if ! grep -q "テスト実行結果" "$latest_report"; then
           verification_issues+=("テスト実行結果分析が不完全です")
       fi
       
       if ! grep -q "カバレッジ分析" "$latest_report"; then
           verification_issues+=("カバレッジ分析が不完全です")
       fi
   fi
   
   # Check metadata updates
   metadata_updated=0
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]] && grep -q "test_results_analysis" "$metadata_file"; then
           metadata_updated=$((metadata_updated + 1))
       fi
   done
   
   if [[ $metadata_updated -eq ${#issue_numbers[@]} ]]; then
       echo "  ✅ メタデータ更新: ${metadata_updated}/${#issue_numbers[@]} 完了"
   else
       verification_issues+=("メタデータ更新が不完全: ${metadata_updated}/${#issue_numbers[@]}")
   fi
   
   # Check agent exit code
   if [[ $agent_exit_code -ne 0 ]]; then
       verification_issues+=("エージェント実行エラー (終了コード: $agent_exit_code)")
   fi
   
   # ✨ Show comprehensive verification results
   show_verification_results "10.5-review-test-results"
   
   # Final decision based on verification results
   if [[ ${#verification_issues[@]} -eq 0 ]]; then
       echo "✅ エージェント結果検証完了"
   else
       echo "❌ エージェント結果検証で問題が発見されました:"
       printf '  - %s\n' "${verification_issues[@]}"
       echo ""
       echo "💡 問題解決のための推奨アクション:"
       echo "  1. テスト実行結果の再確認と必要に応じた修正"
       echo "  2. /run-all-tests ${issue_numbers[*]} でのテスト再実行検討"
       echo "  3. /review-test-results ${issue_numbers[*]} でのレビュー再実行"
       exit 1
   fi
   ```

4. **Display Success Summary**:
   ```bash
   # 🎉 Display comprehensive success summary
   echo ""
   echo "🎉 テスト結果レビュー完了!"
   echo ""
   echo "📊 実行サマリー:"
   echo "  📂 対象Issues: $(printf '#%s ' "${issue_numbers[@]}")"
   echo "  📝 生成レポート: $latest_report"
   echo "  🔄 更新メタデータ: ${metadata_updated} 個"
   echo ""
   
   # Extract and display analysis status from report
   if [[ -f "$latest_report" ]]; then
       analysis_status=$(grep "ステータス:" "$latest_report" | head -1)
       echo "📋 $analysis_status"
       echo ""
       
       # Display next steps based on status
       if grep -q "✅.*APPROVED" "$latest_report"; then
           echo "✅ テスト結果分析完了: リファクタリング推奨"
           echo "💡 次のステップ: /refactor ${issue_numbers[*]}"
       elif grep -q "⚠️.*CONDITIONAL" "$latest_report"; then
           echo "⚠️ 条件付き承認: 軽微な改善推奨"
           echo "💡 次のステップ: 改善適用後に /refactor ${issue_numbers[*]}"
       elif grep -q "❌.*REJECTED" "$latest_report"; then
           echo "❌ 問題修正が必要です"
           echo "💡 次のステップ: 問題修正後に再分析"
           echo ""
           echo "📖 詳細な修正事項については以下のレポートを確認してください:"
           echo "   $latest_report"
           exit 1
       fi
   fi
   
   echo ""
   echo "📁 生成ファイル:"
   echo "  📝 分析レポート: $latest_report"
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]]; then
           echo "  🔄 メタデータ: $metadata_file"
       fi
   done
   echo ""
   echo "🎯 テスト結果レビューが正常に完了しました!"
   ```

**💡 Key Benefits of Test Results Review:**
- **Quality Gate Validation**: Ensure implementation meets quality standards before refactoring
- **Performance Optimization**: Identify bottlenecks and optimization opportunities  
- **Coverage Gap Analysis**: Find untested code paths and critical scenarios
- **Refactoring Safety**: Establish quality baseline before code improvements
- **Continuous Improvement**: Gather metrics for ongoing development process enhancement

**🎯 Critical Success Factors:**
- Focus on coverage gaps - these often reveal design or requirement issues
- Identify performance bottlenecks early to prevent production problems
- Validate all Given-When-Then scenarios are properly implemented
- Ensure quality metrics meet project standards
- Provide clear refactoring roadmap based on analysis results