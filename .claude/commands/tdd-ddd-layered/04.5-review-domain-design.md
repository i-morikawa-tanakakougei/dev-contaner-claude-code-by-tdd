Use the 04.5-review-domain-design subagent to review domain model design for DDD compliance and architectural quality before TDD implementation. This command MUST USE PROACTIVELY the specialized 04.5-review-domain-design subagent for optimal domain design review.

## Metadata
- **Prerequisites**: Domain model design created (04-domain-modeling)
- **Input**: Issue number(s) (required), feature name (optional)
- **Output**: 
  - Domain design review report
  - DDD compliance analysis
  - Updated `docs/use_cases/issue-X-Y.json` metadata with review status
  - Design approval or required modifications
- **Dependencies**: Domain design documents, existing domain models
- **Execution Timing**: After domain modeling, before TDD test creation

## 🎯 **TDD/DDD/LAYERED PROCESS CONTEXT**

**🔄 Core Workflow**: Vision(00) → Structure(01) → Sprint(02) → Use-Case(03) → Domain(04) → **Design Review(04.5)** → Tests(05) → Domain(06) → App(07) → Infra(08) → UI(09) → Test(10) → Refactor(11) → Evolve(12) → Review(13) → Feedback(14) → PR(15) → Status(16)

**🎨 Architecture**: Clean Architecture (Domain→Application→Infrastructure→Presentation)  
**🧪 Development**: Test-Driven Development (RED→GREEN→REFACTOR)  
**🏗️ Design**: Domain-Driven Design (Entity, Value Object, Aggregate, Repository)  
**📋 Requirements**: Given-When-Then scenarios with complete traceability  
**🔄 Evolution**: Continuous scenario evolution via /evolve-scenarios command

> 📖 **Document Management System**: [README.md](./README.md)  
> 🗺️ **Current Position**: Sprint Execution Phase - Domain Design Review (04.5/16)  
> 🎯 **Phase Purpose**: Validate domain design quality before TDD implementation  
> ⬅️ **Previous Stage**: 04-domain-modeling (Domain Model Design)  
> ➡️ **Next Stage**: 05-create-tests (TDD Test Creation)
>
> **📋 3-Layer Architecture Operations**:  
> - 🎯 **Strategic**: `docs/use_cases/core/index.md` (Validate against project vision)  
> - 📊 **Tactical**: `docs/use_cases/index.md` (Update design review status)  
> - 🔧 **Execution**: `docs/use_cases/issue-X-Y.json` (Track design review results)

## 🔍 **DESIGN REVIEW: ANALYSIS ONLY**

**⚠️ Important Notice:**
- **This step is DESIGN REVIEW ONLY** - Analyze and assess domain design quality
- **NO IMPLEMENTATION OR CHANGES** - Focus on evaluation and early problem detection  
- **Quality analysis** - Review DDD compliance, aggregate boundaries, and design decisions
- **Generate design review reports ONLY** - No code or design modifications

**Review Process:**
1. `04-domain-modeling` ← Domain design document created
2. `04.5-review-domain-design` ← **【YOU ARE HERE】Design quality review and validation**
3. `05-create-tests` ← TDD implementation begins (if approved)
4. Alternative: Return to `04-domain-modeling` ← Redesign (if major issues found)

**Critical Early Detection Focus:**
- ✅ DDD aggregate boundary violations (like Issue #3's Trade-Position coupling)
- ✅ Entity/Value Object classification accuracy
- ✅ Business rule placement and encapsulation
- ✅ Integration with existing domain models

## Common Errors and Solutions

### ❌ Error Case 1: DDD aggregate boundary violations
**Cause**: Direct entity references across aggregate boundaries (like Trade→Position)  
**Solution**: 
```bash
# Fix aggregate coupling
- Replace direct object references with string ID references
- Use coordination aggregates for cross-boundary consistency
- Implement domain services for cross-aggregate operations
# Then re-review
/review-domain-design <issue-number>
```

### ❌ Error Case 2: Domain model design document not found
**Cause**: Domain design review attempted before design creation  
**Solution**: 
```bash
# Create domain design first
/domain-modeling <issue-number>
# Then proceed with review
/review-domain-design <issue-number>
```

### ❌ Error Case 3: Inconsistency with existing domain models
**Cause**: New design conflicts with established domain patterns or shared foundation  
**Solution**: 
- Analyze existing domain models for consistency
- Align with shared value objects and patterns
- Consider refactoring existing models if necessary
- Update design to maintain architectural coherence

## Execution Examples

### ✅ Success Example
```bash
$ /review-domain-design 3
🔍 Issues: #3 のドメイン設計レビューを開始します
📋 設計文書分析中...
  📖 分析中: docs/domain/issue-3-domain-model.md
  🎯 ドメイン概念: 3個のエンティティ, 4個の値オブジェクト
📊 DDD準拠性チェック中...
  ✅ 集約境界設計: 適切
  ✅ エンティティ分類: 適切
  ✅ 値オブジェクト設計: 適切
  ✅ ビジネスルール配置: 適切
🔗 既存ドメインとの整合性チェック中...
  ✅ Issue #2共通基盤との互換性: 良好
  ✅ 全体アーキテクチャ一貫性: 維持
📝 設計レビューレポート作成中...
✅ 設計承認: TDD実装開始可能
🎉 ドメイン設計レビュー完了!
```

### ❌ Failure Example and Fix
```bash
$ /review-domain-design 3
🔍 Issues: #3 のドメイン設計レビューを開始します
❌ DDD違反が検出されました:
  ⚠️ Trade → Position 直接参照による集約境界違反
  ⚠️ ビジネスルールの不適切な配置
💡 設計修正が必要です。以下の改善を推奨:
  1. Trade-Position間の文字列ID参照への変更
  2. TradingSession集約による協調管理の追加
  3. 円建て計算ロジックのValue Objectへの移動

# Fix: Address design issues
$ /domain-modeling 3  # Redesign with fixes
$ /review-domain-design 3  # Re-review
```

## 📋 **DOMAIN DESIGN REVIEW TASK CHECKLIST**

**Use this checklist for comprehensive domain design quality assessment:**

### 🔴 Required Tasks

#### **📖 Design Document Analysis**
- [ ] **Verify design document exists**: Confirm `docs/domain/issue-X-Y-domain-model.md` is available
- [ ] **Analyze domain concepts**: Review entities, value objects, and aggregates
- [ ] **Check business rules placement**: Verify invariants and constraints are properly located
- [ ] **Validate ubiquitous language consistency**: Ensure terminology aligns with project standards

#### **🏗️ DDD Compliance Validation**
- [ ] **Verify aggregate boundaries**: Check for proper aggregate root identification and boundaries
- [ ] **Validate entity design**: Ensure entities have identity, lifecycle, and appropriate behavior
- [ ] **Check value object design**: Verify immutability, validation, and equality implementation
- [ ] **Review aggregate references**: Confirm no direct object references across aggregate boundaries

#### **🔗 Integration Analysis**
- [ ] **Check existing domain consistency**: Verify alignment with established domain models
- [ ] **Validate shared foundation usage**: Confirm proper use of common value objects and patterns
- [ ] **Review architectural coherence**: Ensure design maintains overall system architecture

### 🟡 Recommended Tasks

#### **🎯 Business Logic Assessment**
- [ ] **Validate business rule encapsulation**: Check that business logic is properly encapsulated in domain objects
- [ ] **Review domain services design**: Assess complex business logic that spans multiple entities
- [ ] **Check invariant definition**: Verify business invariants are clearly defined and enforceable
- [ ] **Assess calculation logic placement**: Ensure business calculations are in appropriate domain objects

#### **📊 Design Quality Evaluation**
- [ ] **Review entity lifecycle management**: Check entity creation, modification, and deletion patterns
- [ ] **Validate repository interface design**: Assess data access abstraction quality
- [ ] **Check domain event design**: Review event definitions for side effects and integration
- [ ] **Assess extensibility**: Evaluate design flexibility for future requirements

### 🟢 Optional Tasks

#### **🔍 Advanced Design Analysis**
- [ ] **Performance consideration review**: Identify potential performance issues in design
- [ ] **Validate transaction boundaries**: Ensure appropriate transaction scope definition
- [ ] **Review error handling strategy**: Check domain-level error handling and validation
- [ ] **Assess testability**: Evaluate how easily the design can be tested

#### **💰 Domain-Specific Considerations (例: 円建て対応)**
- [ ] **Validate currency handling**: Check proper currency and precision management
- [ ] **Review calculation accuracy**: Ensure financial calculations maintain required precision
- [ ] **Check regulatory compliance**: Verify domain rules align with business regulations
- [ ] **Assess localization support**: Evaluate support for regional business requirements

#### **🔄 Design Evolution Assessment**
- [ ] **Check migration compatibility**: Assess compatibility with existing implementations
- [ ] **Review refactoring requirements**: Identify necessary changes to existing code
- [ ] **Validate backward compatibility**: Ensure design changes don't break existing functionality
- [ ] **Assess impact scope**: Evaluate the scope of changes required across the system

### **📝 Documentation and Communication**
- [ ] **Create design review report**: Generate comprehensive design assessment document
- [ ] **Document design decisions**: Record rationale for key design choices
- [ ] **List required modifications**: Identify specific changes needed before implementation
- [ ] **Generate approval status**: Provide clear go/no-go decision for TDD implementation

**💡 Pro Tip**: Focus on aggregate boundary design - this is where most critical DDD violations occur!

## 📊 **DESIGN REVIEW QUALITY GATES**

### **🟢 APPROVED - Ready for TDD Implementation**
- ✅ All DDD principles properly applied
- ✅ Aggregate boundaries clearly defined and respected
- ✅ Entity/Value Object classification accurate
- ✅ Business rules properly encapsulated
- ✅ Integration with existing domain models validated
- ✅ No architectural violations detected

### **🟡 CONDITIONAL APPROVAL - Minor Issues**
- ⚠️ Minor design improvements recommended
- ⚠️ Documentation enhancements needed
- ⚠️ Optional optimizations suggested
- ✅ Core design principles followed
- ✅ No blocking issues identified

### **🔴 REJECTED - Major Issues Require Redesign**
- ❌ DDD principle violations detected
- ❌ Aggregate boundary problems identified
- ❌ Architectural inconsistencies found
- ❌ Integration conflicts with existing domain
- ❌ Business rule placement errors
- 🔄 **Action**: Return to `/domain-modeling` for redesign

## Task Details

**🤖 Agent Integration**: This command MUST USE PROACTIVELY the specialized `04.5-review-domain-design` subagent for optimal domain design review. Claude Code should automatically delegate this task to the 04.5-review-domain-design subagent based on the command description.

## 📖 Subagent Document Reading Instructions

This command delegates to the specialized `04.5-review-domain-design` subagent.

**MANDATORY: The subagent MUST read these files before execution:**

1. `docs/index.md` - Project overview and current status
2. `.claude/context/project-context.json` - Current project context
3. `/workspace/.claude/context/current-command-context.json` - Current execution context (includes issue numbers)
4. `docs/domain/issue-X-Y-domain-model.md` - Primary domain design document to review
5. `docs/use_cases/issue-X-Y.md` - Use case specifications for alignment validation
6. All existing domain models in `docs/domain/` - For consistency checking
7. `docs/vision/project-vision.md` - Project vision for ubiquitous language validation
8. `docs/use_cases/core/index.md` - Core scenarios for business alignment

**Command-Specific Reading Focus - Domain Design Review:**
- Thoroughly analyze DDD compliance (aggregates, entities, value objects)
- Validate aggregate boundary design and consistency rules
- Review business rule placement and domain logic encapsulation
- Check integration patterns with existing domain models
- Assess ubiquitous language consistency across the domain

**CRITICAL:** Use the Read tool to actually read file contents, not just reference paths.

1. **Pre-execution Validation**:
   ```bash
   # 🔧 Load lightweight structure validation
   source "$(dirname "${BASH_SOURCE[0]}")/_validate_structure.sh"
   
   # Validate issue number requirement
   if [[ $# -eq 0 ]]; then
       echo "エラー: 最低1つのイシュー番号が必要です"
       echo "使用例:"
       echo "  /review-domain-design 3        # 単一イシューの設計レビュー"
       echo "  /review-domain-design 3,15     # 複数イシューの設計レビュー"
       exit 1
   fi
   
   # Parse issue numbers
   IFS=',' read -ra ISSUES <<< "$1"
   echo "🔍 Issues: $(printf '#%s ' "${ISSUES[@]}")のドメイン設計レビューを開始します"
   
   # Execute domain design structure validation
   if ! main "04.5-review-domain-design"; then
       echo "❌ ドメイン設計構造確認が失敗しました"
       exit 1
   fi
   
   echo "✅ 前提条件確認完了: ドメイン設計レビュー準備完了"
   ```

2. **Context Preparation and Agent Execution**:
       feature_name="${other_args[0]}"
   else
       # Feature name will be extracted from existing design files
       feature_name=""
   fi
   
   echo "🔍 Issues: $(printf '#%s ' "${issue_numbers[@]}")のドメイン設計レビューを開始します"
   
   # 📋 Validate that domain design documents exist
   echo "📋 前提条件確認中..."
   
   missing_designs=()
   design_files=()
   
   for issue_num in "${issue_numbers[@]}"; do
       # Search for domain design document
       if [[ -n "$feature_name" ]]; then
           design_file="docs/domain/issue-${issue_num}-${feature_name}-domain-model.md"
       else
           # Find existing design file
           design_file=$(find docs/domain -name "issue-${issue_num}-*-domain-model.md" | head -1)
           if [[ -z "$design_file" ]]; then
               design_file="docs/domain/issue-${issue_num}-domain-model.md"
           fi
       fi
       
       if [[ -f "$design_file" ]]; then
           design_files+=("$design_file")
           echo "  📖 発見: $design_file"
       else
           missing_designs+=("Issue #$issue_num")
       fi
   done
   
   # Check for missing designs
   if [[ ${#missing_designs[@]} -gt 0 ]]; then
       echo "❌ 以下のイシューの設計文書が見つかりません:"
       printf '  - %s\n' "${missing_designs[@]}"
       echo "💡 最初に以下のコマンドでドメイン設計を作成してください:"
       echo "   /domain-modeling ${issue_numbers[*]}"
       exit 1
   fi
   
   echo "✅ 前提条件確認完了: ${#design_files[@]} 個の設計を発見"
   ```

2. **Context Preparation and Agent Execution**:
   ```bash
   # 🔄 Prepare context for agent (Pattern B: Hybrid approach)
   echo "🔍 コンテキスト準備とエージェント起動..."
   
   # Create context file with domain design review information
   context_file="/workspace/.claude/context/current-command-context.json"
   current_time=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
   
   # Build context JSON for domain design review
   cat > "$context_file" <<EOF
   {
     "command": "review-domain-design",
     "timestamp": "$current_time",
     "issue_numbers": [$(IFS=,; echo "${issue_numbers[*]}")],
     "feature_name": "${feature_name:-"auto-detect"}",
     "phase": "domain-design-review",
     "context": {
       "design_files": [$(printf '"%s",' "${design_files[@]}" | sed 's/,$//')]],
       "expected_outputs": [
         "docs/reviews/domain-design-review-$(date +%Y%m%d).md",
         "docs/analysis/ddd-compliance-report.md",
         "docs/domain/ (updated with review results)"
       ],
       "architecture_patterns": ["DDD", "Domain Modeling", "Aggregate Design"]
     },
     "additional_instructions": "ドメイン設計の包括的レビューとDDD準拠性分析を実行してください。エンティティ、値オブジェクト、集約の設計品質、ビジネスルール配置、集約境界の妥当性を評価し、承認・条件付き承認・却下の判定を行ってください。",
     "special_considerations": [
       "DDD原則（エンティティ・値オブジェクト・集約）の厳密な遵守確認",
       "ビジネスルールのドメインオブジェクト内適切配置検証",
       "集約境界と参照関係の設計妥当性評価",
       "既存ドメインとの一貫性と統合可能性確認"
     ],
     "custom_context": {
       "ddd_compliance": true,
       "aggregate_boundary_validation": true,
       "business_logic_placement": true,
       "design_quality_assessment": true
     }
   }
   EOF
   
   echo "✅ コンテキストファイル作成完了: $context_file"
   
   # 🤖 Execute the specialized agent with Task tool
   echo ""
   echo "🔍 ドメイン設計レビューエージェントを起動します..."
   echo "専門エージェントがDDD準拠性と設計品質を評価します"
   echo ""
   
   # Task tool execution with comprehensive prompt
   task_prompt="ドメイン設計レビュータスクを実行してください。

## コンテキスト情報の取得
1. 一時コンテキスト（イシュー・設計情報）:
   - /workspace/.claude/context/current-command-context.json を読み込み

2. ドメイン設計文書の確認:
   - docs/domain/issue-X-Y-domain-model.md で設計内容を確認
   - docs/use_cases/ でビジネス要件との整合性を確認
   - 既存 docs/domain/ で他ドメインとの一貫性を確認

## 実行タスク
1. 設計文書分析（エンティティ・値オブジェクト・集約）
2. DDD準拠性検証（集約境界・識別・ライフサイクル）
3. ビジネスルール配置とカプセル化の妥当性評価
4. ユビキタス言語の一貫性確認
5. 既存ドメインとの統合可能性分析
6. リポジトリインターフェース設計品質評価
7. 承認状況判定（APPROVED/CONDITIONAL_APPROVAL/REJECTED）
8. 包括的レビューレポート生成

## 重要: 標準化出力形式の遵守
レポートは必ず以下の構造化セクションで終了してください：

### 📊 実行サマリー
各Critical Taskの完了状態を✅/❌で明記

### 📋 総合判定
APPROVED/CONDITIONAL_APPROVAL/REJECTED のいずれかを明記

### 💡 次のステップ
判定に基づく具体的なアクションアイテムを列挙

## 処理完了後
- ドメイン設計レビューレポートの作成
- DDD準拠性評価結果の報告
- 次のステップ（テスト作成・設計修正）への案内"

   # Execute with specialized 04.5-review-domain-design subagent
   # The 04.5-review-domain-design subagent will be automatically invoked based on the task description
   
   agent_exit_code=$?
   echo "✅ 専用エージェント実行完了 (終了コード: $agent_exit_code)"
   - Validate backward compatibility maintenance
   - Assess impact scope across the system
   
   📝 Documentation and Communication:
   - Create comprehensive design review report
   - Document design decisions and rationale
   - List required modifications before implementation
   - Generate clear approval status (go/no-go decision)
   
   QUALITY GATES:
   
   🟢 APPROVED - Ready for TDD Implementation:
   - All DDD principles properly applied
   - Aggregate boundaries clearly defined and respected
   - Entity/Value Object classification accurate
   - Business rules properly encapsulated
   - Integration with existing domain models validated
   - No architectural violations detected
   
   🟡 CONDITIONAL APPROVAL - Minor Issues:
   - Minor design improvements recommended
   - Documentation enhancements needed
   - Optional optimizations suggested
   - Core design principles followed
   - No blocking issues identified
   
   🔴 REJECTED - Major Issues Require Redesign:
   - DDD principle violations detected
   - Aggregate boundary problems identified
   - Architectural inconsistencies found
   - Integration conflicts with existing domain
   - Business rule placement errors
   - Action: Return to /domain-modeling for redesign
   
   OUTPUT REQUIREMENTS:
   - Generate comprehensive review report in docs/reviews/
   - Update metadata files with review status
   - Provide clear approval decision with next steps
   - Document specific issues and recommendations
   - Focus on aggregate boundary design (most critical)
   - Ensure early problem detection before TDD implementation"
   
   # Execute the specialized agent
   claude_task="$task_context" \
       claude_agent="04.5-review-domain-design" \
       claude_working_dir="$(pwd)" \
       claude_issues="$(printf '%s,' "${issue_numbers[@]}" | sed 's/,$//')" \
       claude_feature_name="$feature_name" \
       claude --agent
   
   agent_exit_code=$?
   echo "✅ 専用エージェント実行完了 (終了コード: $agent_exit_code)"
   ```

3. **Agent Result Verification**:
   ```bash
   # 🔧 Load advanced task verification library
   source "$(dirname "${BASH_SOURCE[0]}")/_task_verification.sh"
   
   # 🔍 Verify agent execution results with advanced task checking
   echo "🔍 エージェント結果検証中..."
   
   verification_issues=()
   
   # Check for review report generation
   review_report_pattern="docs/reviews/domain-design-review-${issue_list}-*.md"
   review_reports=($(ls $review_report_pattern 2>/dev/null))
   
   if [[ ${#review_reports[@]} -eq 0 ]]; then
       verification_issues+=("設計レビューレポートが生成されていません")
       echo "❌ レビューレポートが見つかりません"
   else
       latest_report="${review_reports[-1]}"  # Get most recent
       echo "  ✅ レビューレポート: $latest_report"
       
       # ✨ New: Advanced task verification with retry capability
       echo "🔍 Critical tasks確認中..."
       if ! verify_critical_tasks "04.5-review-domain-design" "$latest_report"; then
           echo "⚠️ Critical tasks確認で問題が検出されました - 再実行を試行します"
           
           # Prepare retry context with specific issues
           prepare_retry_context "1" "${verification_issues[@]}"
           
           # Execute agent with retry (implement agent call here)
           echo "🔄 エージェント再実行を準備中..."
           echo "💡 手動での再実行が必要です: /review-domain-design ${issue_numbers[*]}"
           
           # For now, show the issues and continue
           echo "❌ 以下の問題が検出されました:"
           for issue in "${verification_issues[@]}"; do
               echo "  - $issue"
           done
       else
           echo "✅ Critical tasks確認完了"
       fi
       
       # Basic content verification (keeping existing checks)
       if ! grep -q "ステータス:" "$latest_report"; then
           verification_issues+=("レビューレポートにステータスが記載されていません")
       fi
       
       if ! grep -q "DDD準拠性評価" "$latest_report"; then
           verification_issues+=("DDD準拠性評価が不完全です")
       fi
   fi
   
   # Check metadata updates
   metadata_updated=0
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]] && grep -q "domain_design_review" "$metadata_file"; then
           metadata_updated=$((metadata_updated + 1))
       fi
   done
   
   if [[ $metadata_updated -eq ${#issue_numbers[@]} ]]; then
       echo "  ✅ メタデータ更新: ${metadata_updated}/${#issue_numbers[@]} 完了"
   else
       verification_issues+=("メタデータ更新が不完全: ${metadata_updated}/${#issue_numbers[@]}")
   fi
   
   # Check agent exit code
   if [[ $agent_exit_code -ne 0 ]]; then
       verification_issues+=("エージェント実行エラー (終了コード: $agent_exit_code)")
   fi
   
   # ✨ Show comprehensive verification results
   show_verification_results "04.5-review-domain-design"
   
   # Final decision based on verification results
   if [[ ${#verification_issues[@]} -eq 0 ]]; then
       echo "✅ エージェント結果検証完了"
   else
       echo "❌ エージェント結果検証で問題が発見されました:"
       printf '  - %s\n' "${verification_issues[@]}"
       echo ""
       echo "💡 問題解決のための推奨アクション:"
       echo "  1. 設計文書の再確認と必要に応じた修正"
       echo "  2. /domain-modeling ${issue_numbers[*]} での再設計検討"
       echo "  3. /review-domain-design ${issue_numbers[*]} でのレビュー再実行"
       exit 1
   fi
   ```

4. **Display Success Summary**:
   ```bash
   # 🎉 Display comprehensive success summary
   echo ""
   echo "🎉 ドメイン設計レビュー完了!"
   echo ""
   echo "📊 実行サマリー:"
   echo "  📂 対象Issues: $(printf '#%s ' "${issue_numbers[@]}")"
   echo "  📖 レビュー済み設計: ${#design_files[@]} 個"
   echo "  📝 生成レポート: $latest_report"
   echo "  🔄 更新メタデータ: ${metadata_updated} 個"
   echo ""
   
   # Extract and display review status from report
   if [[ -f "$latest_report" ]]; then
       review_status=$(grep "ステータス:" "$latest_report" | head -1)
       echo "📋 $review_status"
       echo ""
       
       # Display next steps based on status
       if grep -q "✅.*APPROVED" "$latest_report"; then
           echo "✅ 設計承認: TDD実装開始可能"
           echo "💡 次のステップ: /create-tests ${issue_numbers[*]}"
       elif grep -q "⚠️.*CONDITIONAL" "$latest_report"; then
           echo "⚠️ 条件付き承認: 軽微な改善推奨"
           echo "💡 次のステップ: 改善適用後に /create-tests ${issue_numbers[*]}"
       elif grep -q "❌.*REJECTED" "$latest_report"; then
           echo "❌ 設計修正が必要です"
           echo "💡 次のステップ: /domain-modeling ${issue_numbers[*]} (再設計)"
           echo ""
           echo "📖 詳細な修正事項については以下のレポートを確認してください:"
           echo "   $latest_report"
           exit 1
       fi
   fi
   
   echo ""
   echo "📁 生成ファイル:"
   echo "  📝 レビューレポート: $latest_report"
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]]; then
           echo "  🔄 メタデータ: $metadata_file"
       fi
   done
   echo ""
   echo "🎯 ドメイン設計レビューが正常に完了しました!"
   ```

**💡 Key Benefits of Domain Design Review:**
- **Early Problem Detection**: Catch DDD violations before expensive implementation
- **Architectural Consistency**: Ensure design aligns with existing domain models  
- **Quality Assurance**: Validate proper application of DDD principles
- **Cost Reduction**: Fix design issues before TDD implementation begins
- **Team Alignment**: Ensure common understanding of domain design decisions

**🎯 Critical Success Factors:**
- Focus on aggregate boundary design - most critical for DDD compliance
- Validate integration with existing domain models and shared foundation
- Ensure business rules are properly encapsulated in domain objects
- Check that value objects are properly designed as immutable
- Verify entity identity and lifecycle management is appropriate