TDD実装前にDDD準拠とアーキテクチャ品質についてドメインモデル設計をレビューする

## メタデータ
- **前提条件**: ドメインモデル設計が作成されている（04-domain-modeling）
- **入力**: Issue番号（必須）、機能名（オプション）
- **出力**: 
  - ドメイン設計レビューレポート
  - DDD準拠性分析
  - レビューステータスで更新された`docs/use_cases/issue-X-Y.json`メタデータ
  - 設計承認または必要な修正
- **依存関係**: ドメイン設計文書、既存ドメインモデル
- **実行タイミング**: ドメインモデリング後、TDDテスト作成前

## 🎯 **TDD/DDD/レイヤードプロセスコンテキスト**

**🔄 コアワークフロー**: ビジョン(00) → 構造(01) → スプリント(02) → ユースケース(03) → ドメイン(04) → **設計レビュー(04.5)** → テスト(05) → ドメイン(06) → アプリ(07) → インフラ(08) → UI(09) → テスト(10) → リファクタ(11) → 進化(12) → レビュー(13) → フィードバック(14) → PR(15) → ステータス(16)

**🎨 アーキテクチャ**: クリーンアーキテクチャ（ドメイン→アプリケーション→インフラストラクチャ→プレゼンテーション）  
**🧪 開発**: テスト駆動開発（RED→GREEN→REFACTOR）  
**🏗️ 設計**: ドメイン駆動設計（エンティティ、値オブジェクト、集約、リポジトリ）  
**📋 要件**: 完全なトレーサビリティを持つGiven-When-Thenシナリオ  
**🔄 進化**: /evolve-scenariosコマンドによる継続的シナリオ進化

> 📖 **文書管理システム**: [README.md](./README.md)  
> 🗺️ **現在位置**: スプリント実行フェーズ - ドメイン設計レビュー（04.5/16）  
> 🎯 **フェーズ目的**: TDD実装前のドメイン設計品質検証  
> ⬅️ **前のステージ**: 04-domain-modeling（ドメインモデル設計）  
> ➡️ **次のステージ**: 05-create-tests（TDDテスト作成）
>
> **📋 3層アーキテクチャ操作**:  
> - 🎯 **戦略的**: `docs/use_cases/core/index.md`（プロジェクトビジョンとの検証）  
> - 📊 **戦術的**: `docs/use_cases/index.md`（設計レビューステータスの更新）  
> - 🔧 **実行**: `docs/use_cases/issue-X-Y.json`（設計レビュー結果の追跡）

## 🔍 **設計レビュー: 分析のみ**

**⚠️ 重要な注意:**
- **このステップは設計レビューのみ** - ドメイン設計品質の分析と評価
- **実装や変更なし** - 評価と早期問題検出に集中  
- **品質分析** - DDD準拠、集約境界、設計決定のレビュー
- **設計レビューレポートのみ生成** - コードや設計の修正なし

**レビュープロセス:**
1. `04-domain-modeling` ← ドメイン設計文書が作成された
2. `04.5-review-domain-design` ← **【現在位置】設計品質レビューと検証**
3. `05-create-tests` ← TDD実装開始（承認の場合）
4. 代替: `04-domain-modeling`に戻る ← 再設計（重大な問題が見つかった場合）

**重要な早期検出焦点:**
- ✅ DDD集約境界違反（Issue #3のTrade-Position結合のような）
- ✅ エンティティ/値オブジェクト分類の正確性
- ✅ ビジネスルールの配置とカプセル化
- ✅ 既存ドメインモデルとの統合

## よくあるエラーと解決策

### ❌ エラーケース1: DDD集約境界違反
**原因**: 集約境界を越えた直接エンティティ参照（Trade→Positionのような）  
**解決策**: 
```bash
# 集約結合を修正
- 直接オブジェクト参照を文字列ID参照に置き換える
- 集約間整合性のための協調集約を使用
- 集約間操作のためのドメインサービスを実装
# その後再レビュー
/review-domain-design <issue-number>
```

### ❌ エラーケース2: ドメインモデル設計文書が見つからない
**原因**: 設計作成前にドメイン設計レビューを試行  
**解決策**: 
```bash
# まずドメイン設計を作成
/domain-modeling <issue-number>
# その後レビューを実行
/review-domain-design <issue-number>
```

### ❌ エラーケース3: 既存ドメインモデルとの不整合
**原因**: 新しい設計が確立されたドメインパターンや共有基盤と競合  
**解決策**: 
- 整合性のために既存ドメインモデルを分析
- 共有値オブジェクトとパターンと整合
- 必要に応じて既存モデルのリファクタリングを検討
- アーキテクチャの一貫性を維持するよう設計を更新

## 実行例

### ✅ 成功例
```bash
$ /review-domain-design 3
🔍 Issues: #3 のドメイン設計レビューを開始します
📋 設計文書分析中...
  📖 分析中: docs/domain/issue-3-domain-model.md
  🎯 ドメイン概念: 3個のエンティティ, 4個の値オブジェクト
📊 DDD準拠性チェック中...
  ✅ 集約境界設計: 適切
  ✅ エンティティ分類: 適切
  ✅ 値オブジェクト設計: 適切
  ✅ ビジネスルール配置: 適切
🔗 既存ドメインとの整合性チェック中...
  ✅ Issue #2共通基盤との互換性: 良好
  ✅ 全体アーキテクチャ一貫性: 維持
📝 設計レビューレポート作成中...
✅ 設計承認: TDD実装開始可能
🎉 ドメイン設計レビュー完了!
```

### ❌ 失敗例と修正
```bash
$ /review-domain-design 3
🔍 Issues: #3 のドメイン設計レビューを開始します
❌ DDD違反が検出されました:
  ⚠️ Trade → Position 直接参照による集約境界違反
  ⚠️ ビジネスルールの不適切な配置
💡 設計修正が必要です。以下の改善を推奨:
  1. Trade-Position間の文字列ID参照への変更
  2. TradingSession集約による協調管理の追加
  3. 円建て計算ロジックのValue Objectへの移動

# 修正: 設計問題に対処
$ /domain-modeling 3  # 修正と再設計
$ /review-domain-design 3  # 再レビュー
```

## 📋 **ドメイン設計レビュータスクチェックリスト**

**包括的なドメイン設計品質評価にこのチェックリストを使用してください:**

### 🔴 必須タスク

#### **📖 設計文書分析**
- [ ] **設計文書の存在確認**: `docs/domain/issue-X-Y-domain-model.md`が利用可能であることを確認
- [ ] **ドメイン概念を分析**: エンティティ、値オブジェクト、集約をレビュー
- [ ] **ビジネスルール配置をチェック**: 不変条件と制約が適切に配置されていることを検証
- [ ] **ユビキタス言語一貫性を検証**: 用語がプロジェクト標準と整合していることを確認

#### **🏗️ DDD準拠性検証**
- [ ] **集約境界を検証**: 適切な集約ルート識別と境界をチェック
- [ ] **エンティティ設計を検証**: エンティティがアイデンティティ、ライフサイクル、適切な振る舞いを持つことを確認
- [ ] **値オブジェクト設計をチェック**: 不変性、検証、等価実装を検証
- [ ] **集約参照をレビュー**: 集約境界を越えた直接オブジェクト参照がないことを確認

#### **🔗 統合分析**
- [ ] **既存ドメイン整合性をチェック**: 確立されたドメインモデルとの整合性を検証
- [ ] **共有基盤使用を検証**: 共通値オブジェクトとパターンの適切な使用を確認
- [ ] **アーキテクチャ一貫性をレビュー**: 設計が全体的なシステムアーキテクチャを維持することを確認

### 🟡 推奨タスク

#### **🎯 ビジネスロジック評価**
- [ ] **ビジネスルールカプセル化を検証**: ビジネスロジックがドメインオブジェクトに適切にカプセル化されていることをチェック
- [ ] **ドメインサービス設計をレビュー**: 複数エンティティにまたがる複雑なビジネスロジックを評価
- [ ] **不変条件定義をチェック**: ビジネス不変条件が明確に定義され強制可能であることを検証
- [ ] **計算ロジック配置を評価**: ビジネス計算が適切なドメインオブジェクトにあることを確認

#### **📊 設計品質評価**
- [ ] **エンティティライフサイクル管理をレビュー**: エンティティの作成、変更、削除パターンをチェック
- [ ] **リポジトリインターフェース設計を検証**: データアクセス抽象化品質を評価
- [ ] **ドメインイベント設計をチェック**: 副作用と統合のためのイベント定義をレビュー
- [ ] **拡張性を評価**: 将来の要件に対する設計の柔軟性を評価

### 🟢 オプションタスク

#### **🔍 高度な設計分析**
- [ ] **パフォーマンス考慮事項をレビュー**: 設計での潜在的なパフォーマンス問題を特定
- [ ] **トランザクション境界を検証**: 適切なトランザクションスコープ定義を確認
- [ ] **エラーハンドリング戦略をレビュー**: ドメインレベルのエラーハンドリングと検証をチェック
- [ ] **テスタビリティを評価**: 設計がどれだけ簡単にテストできるかを評価

#### **💰 ドメイン固有考慮事項（例: 円建て対応）**
- [ ] **通貨処理を検証**: 適切な通貨と精度管理をチェック
- [ ] **計算精度をレビュー**: 金融計算が必要な精度を維持することを確認
- [ ] **規制準拠をチェック**: ドメインルールがビジネス規制と整合することを検証
- [ ] **ローカライゼーション対応を評価**: 地域ビジネス要件のサポートを評価

#### **🔄 設計進化評価**
- [ ] **移行互換性をチェック**: 既存実装との互換性を評価
- [ ] **リファクタリング要件をレビュー**: 既存コードへの必要な変更を特定
- [ ] **後方互換性を検証**: 設計変更が既存機能を破壊しないことを確認
- [ ] **影響範囲を評価**: システム全体で必要な変更の範囲を評価

### **📝 文書化とコミュニケーション**
- [ ] **設計レビューレポートを作成**: 包括的な設計評価文書を生成
- [ ] **設計決定を文書化**: 主要な設計選択の根拠を記録
- [ ] **必要な修正をリスト**: 実装前に必要な具体的変更を特定
- [ ] **承認ステータスを生成**: TDD実装に対する明確なgo/no-go決定を提供

**💡 プロTip**: 集約境界設計に焦点を当てる - これは最も重要なDDD違反が発生する場所です！

## 📊 **設計レビュー品質ゲート**

### **🟢 承認 - TDD実装準備完了**
- ✅ すべてのDDD原則が適切に適用されている
- ✅ 集約境界が明確に定義され尊重されている
- ✅ エンティティ/値オブジェクト分類が正確
- ✅ ビジネスルールが適切にカプセル化されている
- ✅ 既存ドメインモデルとの統合が検証されている
- ✅ アーキテクチャ違反が検出されていない

### **🟡 条件付き承認 - 軽微な問題**
- ⚠️ 軽微な設計改善が推奨される
- ⚠️ 文書改善が必要
- ⚠️ オプションの最適化が提案される
- ✅ コア設計原則が守られている
- ✅ ブロッキング問題が特定されていない

### **🔴 却下 - 大きな問題で再設計必要**
- ❌ DDD原則違反が検出された
- ❌ 集約境界問題が特定された
- ❌ アーキテクチャ不整合が発見された
- ❌ 既存ドメインとの統合競合
- ❌ ビジネスルール配置エラー
- 🔄 **アクション**: 再設計のため`/domain-modeling`に戻る

## タスク詳細

1. **安全な環境設定と引数解析**:
   ```bash
   # 🔧 すべての安全操作機能を読み込み
   source "$(dirname "${BASH_SOURCE[0]}")/_setup_safe_environment.sh" "04.5-review-domain-design" "$ARGUMENTS"
   
   # 設計レビューは少なくとも1つのIssue番号を期待
   if [[ ${#issue_numbers[@]} -eq 0 ]]; then
       echo "エラー: 少なくとも1つのイシュー番号を指定してください"
       show_usage_example "review-domain-design" "3" "単一イシューの設計レビュー"
       show_usage_example "review-domain-design" "3,7" "複数イシューの設計レビュー"
       show_usage_example "review-domain-design" "3 trading-execution" "イシュー + 機能名指定"
       exit 1
   fi
   
   # Issueリストと機能名を抽出
   issue_list=$(IFS=-; echo "${issue_numbers[*]}")
   if [[ ${#other_args[@]} -gt 0 ]]; then
       feature_name="${other_args[0]}"
   else
       # 機能名は既存の設計ファイルから抽出される
       feature_name=""
   fi
   
   echo "🔍 Issues: $(printf '#%s ' "${issue_numbers[@]}")のドメイン設計レビューを開始します"
   ```

2. **前提条件の検証と設計文書の特定**:
   ```bash
   # 📋 ドメイン設計文書が存在することを検証
   echo "📋 設計文書分析中..."
   
   missing_designs=()
   design_files=()
   
   for issue_num in "${issue_numbers[@]}"; do
       # ドメイン設計文書を検索
       if [[ -n "$feature_name" ]]; then
           design_file="docs/domain/issue-${issue_num}-${feature_name}-domain-model.md"
       else
           # 既存設計ファイルを検索
           design_file=$(find docs/domain -name "issue-${issue_num}-*-domain-model.md" | head -1)
           if [[ -z "$design_file" ]]; then
               design_file="docs/domain/issue-${issue_num}-domain-model.md"
           fi
       fi
       
       if [[ -f "$design_file" ]]; then
           design_files+=("$design_file")
           echo "  📖 分析中: $design_file"
       else
           missing_designs+=("Issue #$issue_num")
       fi
   done
   
   # 不足設計をチェック
   if [[ ${#missing_designs[@]} -gt 0 ]]; then
       echo "❌ 以下のイシューの設計文書が見つかりません:"
       printf '  - %s\n' "${missing_designs[@]}"
       echo "💡 最初に以下のコマンドでドメイン設計を作成してください:"
       echo "   /domain-modeling ${issue_numbers[*]}"
       exit 1
   fi
   
   echo "✅ 設計文書確認完了: ${#design_files[@]} 個の設計を発見"
   ```

3. **ドメイン設計文書の分析**:
   ```bash
   # 🎯 ドメイン概念を抽出・分析
   echo "🎯 ドメイン概念分析中..."
   
   total_entities=0
   total_value_objects=0
   total_aggregates=0
   design_issues=()
   
   for design_file in "${design_files[@]}"; do
       # ドメイン概念をカウント
       entities=$(grep -c "^- \*\*.*Entity\*\*" "$design_file" 2>/dev/null || echo "0")
       value_objects=$(grep -c "^- \*\*.*Value.*Object\*\*\|^- \*\*.*値オブジェクト\*\*" "$design_file" 2>/dev/null || echo "0")
       aggregates=$(grep -c "^- \*\*.*Aggregate\*\*\|^- \*\*.*集約\*\*" "$design_file" 2>/dev/null || echo "0")
       
       total_entities=$((total_entities + entities))
       total_value_objects=$((total_value_objects + value_objects))
       total_aggregates=$((total_aggregates + aggregates))
       
       echo "  📊 $design_file: ${entities}個のエンティティ, ${value_objects}個の値オブジェクト, ${aggregates}個の集約"
   done
   
   echo "  🎯 ドメイン概念: ${total_entities}個のエンティティ, ${total_value_objects}個の値オブジェクト, ${total_aggregates}個の集約"
   ```

4. **DDD準拠性分析**:
   ```bash
   # 📊 DDD準拠性をチェック
   echo "📊 DDD準拠性チェック中..."
   
   ddd_violations=()
   ddd_warnings=()
   
   # 集約境界違反をチェック
   for design_file in "${design_files[@]}"; do
       # 直接エンティティ参照を探す（一般的な違反）
       if grep -q "直接参照\|direct reference\|entity.*\..*entity" "$design_file" 2>/dev/null; then
           ddd_violations+=("集約境界違反の可能性: $design_file")
       fi
       
       # 適切なビジネスルール配置をチェック
       if ! grep -q "ビジネスルール\|business rule\|invariant" "$design_file" 2>/dev/null; then
           ddd_warnings+=("ビジネスルール定義不足: $design_file")
       fi
       
       # 値オブジェクト不変性の言及をチェック
       if ! grep -q "不変\|immutable\|readonly" "$design_file" 2>/dev/null; then
           ddd_warnings+=("値オブジェクト不変性の明記不足: $design_file")
       fi
   done
   
   # DDD準拠ステータスをレポート
   if [[ ${#ddd_violations[@]} -eq 0 ]]; then
       echo "  ✅ 集約境界設計: 適切"
       echo "  ✅ エンティティ分類: 適切"
       echo "  ✅ 値オブジェクト設計: 適切"
       echo "  ✅ ビジネスルール配置: 適切"
   else
       echo "  ❌ DDD違反が検出されました:"
       printf '    ⚠️ %s\n' "${ddd_violations[@]}"
   fi
   
   if [[ ${#ddd_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ DDD改善推奨事項:"
       printf '    💡 %s\n' "${ddd_warnings[@]}"
   fi
   ```

5. **統合と整合性分析**:
   ```bash
   # 🔗 既存ドメインモデルとの統合をチェック
   echo "🔗 既存ドメインとの整合性チェック中..."
   
   integration_issues=()
   
   # 共有基盤との整合性をチェック
   if [[ -d "src/domain/shared" ]]; then
       echo "  ✅ Issue #2共通基盤との互換性: 確認中"
       
       # 設計が共有値オブジェクトに言及しているかチェック
       shared_usage_count=0
       for design_file in "${design_files[@]}"; do
           if grep -q "Money\|Price\|Symbol\|AccountCurrency" "$design_file"; then
               shared_usage_count=$((shared_usage_count + 1))
           fi
       done
       
       if [[ $shared_usage_count -gt 0 ]]; then
           echo "  ✅ 共通基盤活用: ${shared_usage_count}個の設計で確認"
       else
           integration_issues+=("共通基盤の活用が不十分")
       fi
   fi
   
   # アーキテクチャ整合性をチェック
   existing_designs=$(find docs/domain -name "*-domain-model.md" | wc -l)
   echo "  📊 既存設計との整合性: ${existing_designs}個の既存設計と比較"
   
   if [[ ${#integration_issues[@]} -eq 0 ]]; then
       echo "  ✅ 全体アーキテクチャ一貫性: 維持"
   else
       echo "  ⚠️ 統合課題:"
       printf '    💡 %s\n' "${integration_issues[@]}"
   fi
   ```

6. **レビューレポートと決定の生成**:
   ```bash
   # 📝 包括的レビューレポートを生成
   echo "📝 設計レビューレポート作成中..."
   
   review_report="docs/reviews/domain-design-review-${issue_list}-$(date +%Y%m%d).md"
   mkdir -p "$(dirname "$review_report")"
   
   # 全体ステータスを決定
   if [[ ${#ddd_violations[@]} -eq 0 && ${#integration_issues[@]} -eq 0 ]]; then
       review_status="APPROVED"
       status_icon="✅"
       next_action="TDD実装開始可能"
   elif [[ ${#ddd_violations[@]} -eq 0 ]]; then
       review_status="CONDITIONAL_APPROVAL"
       status_icon="⚠️"
       next_action="軽微な改善後にTDD実装可能"
   else
       review_status="REJECTED"
       status_icon="❌"
       next_action="設計修正が必要"
   fi
   
   # レビューレポートを作成
   cat > "$review_report" << EOF
# ドメイン設計レビューレポート

**Issues**: $(printf '#%s ' "${issue_numbers[@]}")  
**レビュー日時**: $(date)  
**ステータス**: $status_icon $review_status  

## 設計分析結果

- **エンティティ**: ${total_entities}個
- **値オブジェクト**: ${total_value_objects}個  
- **集約**: ${total_aggregates}個

## DDD準拠性評価

$(if [[ ${#ddd_violations[@]} -eq 0 ]]; then
    echo "✅ **DDD原則準拠**: 全ての基本原則が適切に適用されています"
else
    echo "❌ **DDD違反検出**: 以下の問題が発見されました"
    printf '- %s\n' "${ddd_violations[@]}"
fi)

## 統合性評価

$(if [[ ${#integration_issues[@]} -eq 0 ]]; then
    echo "✅ **統合性良好**: 既存ドメインとの整合性が保たれています"
else
    echo "⚠️ **統合課題**: 以下の改善が推奨されます"
    printf '- %s\n' "${integration_issues[@]}"
fi)

## 推奨アクション

**次のステップ**: $next_action

$(if [[ "$review_status" == "REJECTED" ]]; then
    echo "### 必要な修正"
    echo "1. DDD違反の修正"
    printf '   - %s\n' "${ddd_violations[@]}"
    echo "2. 設計の再検討と更新"
    echo "3. レビューの再実行"
fi)

$(if [[ ${#ddd_warnings[@]} -gt 0 ]]; then
    echo "### 改善推奨事項"
    printf '- %s\n' "${ddd_warnings[@]}"
fi)
EOF
   
   echo "✅ レビューレポート作成完了: $review_report"
   ```

7. **メタデータの更新と決定の提供**:
   ```bash
   # 🔄 レビュー結果でメタデータを更新
   echo "🔄 メタデータ更新中..."
   
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]]; then
           # メタデータでレビューステータスを更新
           update_metadata_atomic "$metadata_file" \
               --set ".phases.domain_design_review.reviewed = true" \
               --set ".phases.domain_design_review.review_status = \"$review_status\"" \
               --set ".phases.domain_design_review.review_date = \"$(date -Iseconds)\"" \
               --set ".phases.domain_design_review.review_report = \"$review_report\""
       fi
   done
   
   # 最終決定と次のステップを提供
   echo ""
   echo "$status_icon 設計レビュー結果: $review_status"
   
   case "$review_status" in
       "APPROVED")
           echo "✅ 設計承認: TDD実装開始可能"
           echo "💡 次のステップ: /create-tests ${issue_numbers[*]}"
           ;;
       "CONDITIONAL_APPROVAL")
           echo "⚠️ 条件付き承認: 軽微な改善推奨"
           echo "💡 次のステップ: 改善適用後に /create-tests ${issue_numbers[*]}"
           ;;
       "REJECTED")
           echo "❌ 設計修正が必要です"
           echo "💡 次のステップ: /domain-modeling ${issue_numbers[*]} (再設計)"
           echo ""
           echo "主な修正事項:"
           printf '  - %s\n' "${ddd_violations[@]}"
           exit 1
           ;;
   esac
   
   echo "🎉 ドメイン設計レビュー完了!"
   ```

**💡 ドメイン設計レビューの主要メリット:**
- **早期問題検出**: 高コストな実装前にDDD違反をキャッチ
- **アーキテクチャ整合性**: 設計が既存ドメインモデルと整合することを確保  
- **品質保証**: DDD原則の適切な適用を検証
- **コスト削減**: TDD実装開始前に設計問題を修正
- **チーム整合**: ドメイン設計決定の共通理解を確保

**🎯 重要な成功要因:**
- 集約境界設計に焦点を当てる - DDD準拠で最も重要
- 既存ドメインモデルと共有基盤との統合を検証
- ビジネスルールがドメインオブジェクトに適切にカプセル化されていることを確認
- 値オブジェクトが不変として適切に設計されていることをチェック
- エンティティのアイデンティティとライフサイクル管理が適切であることを検証