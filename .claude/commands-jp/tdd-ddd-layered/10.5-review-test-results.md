包括的なテスト実行結果をレビューし、リファクタリング前に品質メトリクスを分析します。

## メタデータ
- **前提条件**: すべてのテストが実行され結果が利用可能であること（10-run-all-tests）
- **入力**: イシュー番号（必須）、機能名（オプション）
- **出力**: 
  - テスト結果分析レポート
  - 品質メトリクス評価
  - テスト分析結果を含む `docs/use_cases/issue-X-Y.json` メタデータの更新
  - リファクタリング推奨事項または必要な修正
- **依存関係**: テスト実行結果、カバレッジレポート、実装コード
- **実行タイミング**: テスト実行後、リファクタリング前

## 🎯 **TDD/DDD/レイヤードアーキテクチャプロセスコンテキスト**

**🔄 コアワークフロー**: ビジョン(00) → 構造(01) → スプリント(02) → スプリントレビュー(02.5) → ユースケース(03) → ドメイン(04) → 設計レビュー(04.5) → テスト(05) → テストレビュー(05.5) → ドメイン(06) → アプリ(07) → インフラ(08) → UI(09) → テスト(10) → **テスト結果レビュー(10.5)** → リファクタ(11) → 進化(12) → レビュー(13) → フィードバック(14) → PR(15) → ステータス(16)

**🎨 アーキテクチャ**: クリーンアーキテクチャ（ドメイン→アプリケーション→インフラストラクチャ→プレゼンテーション）  
**🧪 開発手法**: テスト駆動開発（RED→GREEN→REFACTOR）  
**🏗️ 設計手法**: ドメイン駆動設計（エンティティ、値オブジェクト、集約、リポジトリ）  
**📋 要件管理**: Given-When-Thenシナリオによる完全なトレーサビリティ  
**🔄 進化**: /evolve-scenariosコマンドによる継続的シナリオ進化

> 📖 **ドキュメント管理システム**: [README.md](./README.md)  
> 🗺️ **現在位置**: スプリント実行フェーズ - テスト結果レビュー（10.5/16）  
> 🎯 **フェーズ目的**: リファクタリング前のテスト結果と品質メトリクスの分析  
> ⬅️ **前段階**: 10-run-all-tests（包括的テスト実行）  
> ➡️ **次段階**: 11-refactor（リファクタリング）
>
> **📋 3層アーキテクチャ操作**:  
> - 🎯 **戦略**: `docs/use_cases/core/index.md`（シナリオに対する検証）  
> - 📊 **戦術**: `docs/use_cases/index.md`（品質ステータスの更新）  
> - 🔧 **実行**: `docs/use_cases/issue-X-Y.json`（テスト分析結果の追跡）

## 🔍 **テスト結果レビュー: 分析のみ**

**⚠️ 重要な注意事項:**
- **このステップはテスト分析のみ** - テスト実行結果の分析と評価
- **実装変更は行わない** - 評価と品質アセスメントに焦点を当てる
- **品質分析** - テストカバレッジ、パフォーマンスメトリクス、コード品質のレビュー
- **分析レポートのみ生成** - コード修正は行わない

**レビュープロセス:**
1. `10-run-all-tests` ← すべてのテストが実行され結果が生成済み
2. `10.5-review-test-results` ← **【現在地】テスト結果分析と品質評価**
3. `11-refactor` ← リファクタリング開始（承認された場合）
4. 代替: 重要な問題の修正 ← ブロッキング問題への対処（重大な問題が発見された場合）

**重要なテスト分析フォーカス:**
- ✅ テストカバレッジ分析とギャップの特定
- ✅ パフォーマンスボトルネックと最適化機会
- ✅ 品質メトリクス評価（複雑度、保守性）
- ✅ アーキテクチャ準拠の検証

## よくあるエラーと解決策

### ❌ エラーケース1: テスト実行結果が見つからない
**原因**: テスト実行前にテスト結果レビューを試行  
**解決策**: 
```bash
# 最初にテストを実行
/run-all-tests <issue-number>
# その後レビューを実行
/review-test-results <issue-number>
```

### ❌ エラーケース2: 低いテストカバレッジが検出された
**原因**: 実装に適切なテストカバレッジがない  
**解決策**: 
- カバーされていないコードパスを特定してテストを追加
- 重要なビジネスロジックのカバレッジに焦点を当てる
- Given-When-Thenシナリオが完全にテストされていることを確認

### ❌ エラーケース3: パフォーマンス問題が検出された
**原因**: 実装にパフォーマンスボトルネックがある  
**解決策**: 
- 遅いテストを分析し、重要なパスを最適化
- データベースクエリと外部依存関係を確認
- 高コストな処理のキャッシュ戦略を検討

## 実行例

### ✅ 成功例
```bash
$ /review-test-results 3
🔍 Issues: #3 のテスト結果レビューを開始します
📊 テスト実行結果分析中...
  📈 テスト成功率: 100% (45/45テスト)
  📊 カバレッジ分析: 85% (target: 80%+)
  ⏱️ 実行時間: 2.3秒 (acceptable)
🎯 Given-When-Thenカバレッジ確認中...
  ✅ シナリオ実装率: 100% (5/5シナリオ)
  ✅ 受け入れ基準: すべて満たしている
📈 品質メトリクス評価中...
  ✅ 循環複雑度: 平均3.2 (good)
  ✅ 保守性指数: 82 (high)
  ✅ アーキテクチャ準拠: DDD/Clean違反なし
✅ テスト結果分析完了: リファクタリング推奨
🎉 テスト結果レビュー完了!
```

### ❌ 失敗例と修正
```bash
$ /review-test-results 3
🔍 Issues: #3 のテスト結果レビューを開始します
❌ テスト結果に問題が検出されました:
  ⚠️ カバレッジ不足: 65% (target: 80%+)
  ⚠️ 性能問題: 実行時間15秒 (slow tests detected)
  ⚠️ 複雑度警告: 3個のメソッドで複雑度10+
💡 リファクタリング前に修正が必要です:
  1. カバレッジ向上: 未テスト箇所の特定と追加
  2. 性能改善: 遅いテストの最適化
  3. 複雑度削減: 高複雑度メソッドの分割

# 修正: リファクタリング前に問題に対処
$ # テストを追加してパフォーマンスを最適化
$ /review-test-results 3  # 再レビュー
```

## 📋 **テスト結果レビュータスクチェックリスト**

**包括的なテスト結果品質評価のためのチェックリストを使用してください:**

### 🔴 必須タスク

#### **📊 テスト実行結果分析**
- [ ] **テスト成功率の確認**: 全体のテスト合格/不合格比率をチェック
- [ ] **失敗テストの分析**: 失敗したテストと根本原因を調査
- [ ] **テスト実行時間の確認**: テストスイートのパフォーマンスを評価
- [ ] **テスト安定性のチェック**: テストが一貫して動作し、不安定な失敗がないことを確認

#### **📈 カバレッジ分析**
- [ ] **カバレッジレポートの生成**: カバレッジ分析ツールを実行
- [ ] **ラインカバレッジの分析**: 層別のコードカバレッジ率を確認
- [ ] **分岐カバレッジの評価**: 決定パスのカバレッジを評価
- [ ] **カバレッジギャップの特定**: 重要なテストされていないコードパスを発見

#### **🎯 シナリオ実装検証**
- [ ] **テストとシナリオのマッピング**: すべてのGiven-When-Thenシナリオが実装されているか確認
- [ ] **受け入れ基準のチェック**: すべての受け入れ基準が検証されていることを確認
- [ ] **ビジネスルールテストの確認**: ビジネスルールが徹底的にテストされていることを確認
- [ ] **エラーシナリオカバレッジの検証**: エラーケースが適切にテストされているかチェック

### 🟡 推奨タスク

#### **📈 品質メトリクス評価**
- [ ] **コード複雑度の分析**: 循環複雑度メトリクスを確認
- [ ] **保守性の評価**: 保守性指数スコアを評価
- [ ] **コード重複のチェック**: リファクタリングが必要な重複コードを特定
- [ ] **アーキテクチャ準拠の確認**: クリーンアーキテクチャとDDD原則を検証

#### **⚡ パフォーマンス分析**
- [ ] **遅いテストの特定**: 実行時間が長いテストを発見
- [ ] **ボトルネックの分析**: パフォーマンスプロファイリング結果を確認
- [ ] **リソース使用量のチェック**: メモリとCPU使用パターンを評価
- [ ] **スケーラビリティの評価**: 負荷下でのパフォーマンスを検討

### 🟢 オプションタスク

#### **🔍 高度な品質評価**
- [ ] **セキュリティ脆弱性スキャン**: 潜在的なセキュリティ問題をチェック
- [ ] **依存関係分析**: サードパーティ依存関係とバージョンを確認
- [ ] **技術的負債評価**: 技術的負債削減が必要な領域を特定
- [ ] **ドキュメントカバレッジ**: パブリックAPIに適切なドキュメントがあることを確認

#### **💰 ドメイン固有分析（例: 円建て対応）**
- [ ] **金融計算精度**: 金銭計算の精度を検証
- [ ] **通貨処理検証**: JPY固有の計算が正しいかチェック
- [ ] **規制コンプライアンスチェック**: 金融規制が満たされていることを確認
- [ ] **エッジケースカバレッジ**: 金融エッジケースがテストされていることを確認

#### **🔄 継続的改善評価**
- [ ] **テストスイート進化**: テストが要件とともにどう進化できるかを評価
- [ ] **自動化機会**: テスト自動化改善領域を特定
- [ ] **品質ゲート有効性**: 現在の品質ゲートと基準を評価
- [ ] **チーム生産性への影響**: 開発速度への影響を検討

### **📝 ドキュメントとコミュニケーション**
- [ ] **テスト分析レポートの作成**: 包括的なテスト結果評価を生成
- [ ] **品質メトリクスの文書化**: 現在の品質ベースラインを記録
- [ ] **改善機会の一覧化**: 強化すべき特定領域を特定
- [ ] **リファクタリングロードマップの生成**: 改善のための明確な次のステップを提供

**💡 プロのヒント**: カバレッジギャップとパフォーマンスボトルネックに焦点を当てる - これらはしばしば設計上の問題を明らかにします！

## 📊 **テスト結果品質ゲート**

### **🟢 承認 - リファクタリング準備完了**
- ✅ 高いテスト成功率（95%以上が合格）
- ✅ 適切なテストカバレッジ（80%以上のラインカバレッジ）
- ✅ 良好なパフォーマンスメトリクス（妥当な実行時間）
- ✅ 完全なシナリオ実装
- ✅ 受け入れ可能な品質メトリクス（複雑度、保守性）
- ✅ 重要なアーキテクチャ違反なし

### **🟡 条件付き承認 - 軽微な問題**
- ⚠️ 軽微なカバレッジギャップ（70-80%カバレッジ）
- ⚠️ 一部のパフォーマンス最適化機会
- ⚠️ 品質改善推奨
- ✅ コア機能は正しく動作
- ✅ ブロッキングな品質問題は特定されず

### **🔴 拒否 - 重要な問題で修正が必要**
- ❌ 低いテスト成功率（< 95%）
- ❌ 不適切なテストカバレッジ（< 70%）
- ❌ 重大なパフォーマンス問題
- ❌ シナリオ実装の欠落
- ❌ 高いコード複雑度または保守性の問題
- 🔄 **アクション**: リファクタリング前に重要な問題を修正

## タスクの詳細

1. **安全な環境のセットアップと引数解析**:
   ```bash
   # 🔧 すべての安全操作関数を読み込み
   source "$(dirname "${BASH_SOURCE[0]}")/_setup_safe_environment.sh" "10.5-review-test-results" "$ARGUMENTS"
   
   # テスト結果レビューは最低1つのイシュー番号が必要
   if [[ ${#issue_numbers[@]} -eq 0 ]]; then
       echo "エラー: 少なくとも1つのイシュー番号を指定してください"
       show_usage_example "review-test-results" "3" "単一イシューのテスト結果レビュー"
       show_usage_example "review-test-results" "3,7" "複数イシューのテスト結果レビュー"
       show_usage_example "review-test-results" "3 trading-execution" "イシュー + 機能名指定"
       exit 1
   fi
   
   # イシューリストと機能名の抽出
   issue_list=$(IFS=-; echo "${issue_numbers[*]}")
   if [[ ${#other_args[@]} -gt 0 ]]; then
       feature_name="${other_args[0]}"
   else
       # 機能名は既存のメタデータファイルから抽出される
       feature_name=""
   fi
   
   echo "🔍 Issues: $(printf '#%s ' "${issue_numbers[@]}")のテスト結果レビューを開始します"
   ```

2. **前提条件の検証とテスト結果の収集**:
   ```bash
   # 📊 テスト実行結果が存在することを検証
   echo "📊 テスト実行結果分析中..."
   
   # testsディレクトリが存在するかチェック
   if [[ ! -d "tests/" ]]; then
       echo "❌ テストディレクトリが見つかりません"
       echo "💡 最初に以下のコマンドでテストを実行してください:"
       echo "   /run-all-tests ${issue_numbers[*]}"
       exit 1
   fi
   
   # 現在の結果を取得するためテストを実行
   echo "  🧪 テスト実行中..."
   test_output=$(uv run --frozen pytest tests/ -v --tb=short 2>&1 || echo "")
   
   # テストメトリクスの抽出
   total_tests=$(echo "$test_output" | grep -E "=+ .* =+$" | grep -oE "[0-9]+ passed|[0-9]+ failed|[0-9]+ error" | awk '{sum += $1} END {print sum}')
   passed_tests=$(echo "$test_output" | grep -oE "[0-9]+ passed" | grep -oE "[0-9]+" || echo "0")
   failed_tests=$(echo "$test_output" | grep -oE "[0-9]+ failed" | grep -oE "[0-9]+" || echo "0")
   error_tests=$(echo "$test_output" | grep -oE "[0-9]+ error" | grep -oE "[0-9]+" || echo "0")
   
   # 成功率の計算
   if [[ $total_tests -gt 0 ]]; then
       success_rate=$(( (passed_tests * 100) / total_tests ))
       echo "  📈 テスト成功率: ${success_rate}% (${passed_tests}/${total_tests}テスト)"
   else
       echo "  ❌ テスト結果が取得できません"
       echo "💡 テストを実行してください: /run-all-tests ${issue_numbers[*]}"
       exit 1
   fi
   
   # 実行時間の取得
   execution_time=$(echo "$test_output" | grep -oE "in [0-9]+\.[0-9]+s" | grep -oE "[0-9]+\.[0-9]+" || echo "unknown")
   echo "  ⏱️ 実行時間: ${execution_time}秒"
   ```

3. **カバレッジ分析**:
   ```bash
   # 📊 テストカバレッジを分析
   echo "📊 カバレッジ分析中..."
   
   coverage_issues=()
   coverage_warnings=()
   
   # カバレッジレポートの生成
   echo "  📈 カバレッジレポート生成中..."
   coverage_output=$(uv run --frozen pytest --cov=src --cov-report=term 2>&1 || echo "")
   
   # カバレッジ率の抽出
   if echo "$coverage_output" | grep -q "TOTAL"; then
       total_coverage=$(echo "$coverage_output" | grep "TOTAL" | awk '{print $4}' | sed 's/%//')
       echo "  📊 カバレッジ分析: ${total_coverage}% (target: 80%+)"
       
       if [[ $total_coverage -ge 80 ]]; then
           echo "  ✅ カバレッジ目標達成"
       elif [[ $total_coverage -ge 70 ]]; then
           coverage_warnings+=("カバレッジ改善推奨: ${total_coverage}%")
       else
           coverage_issues+=("カバレッジ不足: ${total_coverage}% (target: 80%+)")
       fi
   else
       coverage_warnings+=("カバレッジ情報を取得できませんでした")
   fi
   
   # 層別のカバレッジ分析
   layers=("domain" "application" "infrastructure" "presentation")
   for layer in "${layers[@]}"; do
       if [[ -d "src/$layer" ]]; then
           layer_coverage=$(echo "$coverage_output" | grep "src/$layer" | awk '{print $4}' | sed 's/%//' | head -1)
           if [[ -n "$layer_coverage" ]]; then
               echo "    - ${layer}層: ${layer_coverage}%"
               if [[ $layer_coverage -lt 70 ]]; then
                   coverage_warnings+=("${layer}層のカバレッジが低いです: ${layer_coverage}%")
               fi
           fi
       fi
   done
   
   # カバレッジステータスの報告
   if [[ ${#coverage_issues[@]} -eq 0 ]]; then
       echo "  ✅ カバレッジ分析: 良好"
   else
       echo "  ❌ カバレッジ問題が検出されました:"
       printf '    ⚠️ %s\n' "${coverage_issues[@]}"
   fi
   
   if [[ ${#coverage_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ カバレッジ改善推奨事項:"
       printf '    💡 %s\n' "${coverage_warnings[@]}"
   fi
   ```

4. **Given-When-Thenシナリオ検証**:
   ```bash
   # 🎯 Given-When-Thenシナリオ実装の検証
   echo "🎯 Given-When-Thenカバレッジ確認中..."
   
   scenario_issues=()
   scenario_warnings=()
   
   # ユースケース仕様ファイルの検索
   spec_files=()
   for issue_num in "${issue_numbers[@]}"; do
       spec_file=$(find docs/use_cases/ -name "*issue-*${issue_num}*" -type f -name "*.md" | head -1)
       if [[ -n "$spec_file" ]]; then
           spec_files+=("$spec_file")
       fi
   done
   
   if [[ ${#spec_files[@]} -gt 0 ]]; then
       total_scenarios=0
       implemented_scenarios=0
       
       for spec_file in "${spec_files[@]}"; do
           # Given-When-Thenシナリオのカウント
           gwt_count=$(grep -c "Given:\|When:\|Then:" "$spec_file" 2>/dev/null || echo "0")
           scenario_count=$((gwt_count / 3))  # 各シナリオに3つの部分があると仮定
           total_scenarios=$((total_scenarios + scenario_count))
           
           # シナリオがテストで実装されているかチェック
           scenario_text=$(grep -A 2 -B 2 "Given:\|When:\|Then:" "$spec_file" 2>/dev/null || echo "")
           while IFS= read -r line; do
               if [[ -n "$line" ]]; then
                   # テスト実装を検索
                   test_search=$(echo "$line" | sed 's/Given:\|When:\|Then://g' | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/_/g' | cut -c1-15)
                   if find tests/ -name "*.py" -exec grep -l "$test_search\|test.*$(echo "$test_search" | cut -c1-8)" {} \; 2>/dev/null | head -1 >/dev/null; then
                       implemented_scenarios=$((implemented_scenarios + 1))
                   fi
               fi
           done <<< "$scenario_text"
       done
       
       # 実装率の計算
       if [[ $total_scenarios -gt 0 ]]; then
           implementation_rate=$(( (implemented_scenarios * 100) / total_scenarios ))
           echo "  📊 シナリオ実装率: ${implementation_rate}% (${implemented_scenarios}/${total_scenarios}シナリオ)"
           
           if [[ $implementation_rate -ge 90 ]]; then
               echo "  ✅ 受け入れ基準: すべて満たしている"
           elif [[ $implementation_rate -ge 80 ]]; then
               scenario_warnings+=("一部シナリオの実装が不完全: ${implementation_rate}%")
           else
               scenario_issues+=("シナリオ実装率が不足: ${implementation_rate}%")
           fi
       else
           scenario_warnings+=("Given-When-Thenシナリオが特定できません")
       fi
   else
       scenario_warnings+=("ユースケース仕様ファイルが見つかりません")
   fi
   
   # シナリオ検証ステータスの報告
   if [[ ${#scenario_issues[@]} -eq 0 ]]; then
       echo "  ✅ シナリオ実装: 完了"
   else
       echo "  ❌ シナリオ実装問題が検出されました:"
       printf '    ⚠️ %s\n' "${scenario_issues[@]}"
   fi
   
   if [[ ${#scenario_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ シナリオ実装改善推奨事項:"
       printf '    💡 %s\n' "${scenario_warnings[@]}"
   fi
   ```

5. **品質メトリクス評価**:
   ```bash
   # 📈 コード品質メトリクスの評価
   echo "📈 品質メトリクス評価中..."
   
   quality_issues=()
   quality_warnings=()
   
   # ruff（コード品質ツール）のチェック
   if command -v ruff >/dev/null 2>&1; then
       echo "  🔍 コード品質分析中..."
       ruff_output=$(uv run --frozen ruff check src/ --output-format=text 2>&1 || echo "")
       
       # 違反数のカウント
       violation_count=$(echo "$ruff_output" | grep -c ":" || echo "0")
       
       if [[ $violation_count -eq 0 ]]; then
           echo "  ✅ コード品質: 違反なし"
       elif [[ $violation_count -le 10 ]]; then
           quality_warnings+=("軽微なコード品質違反: ${violation_count}件")
       else
           quality_issues+=("多数のコード品質違反: ${violation_count}件")
       fi
   else
       quality_warnings+=("コード品質チェックツールが利用できません")
   fi
   
   # 簡単な複雑度分析（複雑なメソッドのカウント）
   complex_methods=0
   if [[ -d "src/" ]]; then
       # 多くの条件文を持つメソッドを探す（大まかな複雑度推定）
       complex_methods=$(find src/ -name "*.py" -exec grep -c "if.*elif\|for.*if\|while.*if\|try.*except.*except" {} \; 2>/dev/null | awk '{sum += $1} END {print sum}' || echo "0")
       
       if [[ $complex_methods -eq 0 ]]; then
           echo "  ✅ 循環複雑度: 良好"
       elif [[ $complex_methods -le 5 ]]; then
           echo "  ⚠️ 循環複雑度: 軽微な複雑性あり (${complex_methods}箇所)"
           quality_warnings+=("複雑度改善推奨: ${complex_methods}個のメソッド")
       else
           quality_issues+=("高複雑度メソッド: ${complex_methods}個")
       fi
   fi
   
   # アーキテクチャ準拠チェック（簡略化）
   arch_violations=0
   if [[ -d "src/domain" ]]; then
       # ドメイン層のインフラストラクチャ依存をチェック
       domain_violations=$(find src/domain -name "*.py" -exec grep -l "import.*infrastructure\|from.*infrastructure" {} \; 2>/dev/null | wc -l)
       arch_violations=$((arch_violations + domain_violations))
   fi
   
   if [[ $arch_violations -eq 0 ]]; then
       echo "  ✅ アーキテクチャ準拠: DDD/Clean違反なし"
   else
       quality_issues+=("アーキテクチャ違反: ${arch_violations}件")
   fi
   
   # 品質メトリクスステータスの報告
   if [[ ${#quality_issues[@]} -eq 0 ]]; then
       echo "  ✅ 品質メトリクス: 良好"
   else
       echo "  ❌ 品質問題が検出されました:"
       printf '    ⚠️ %s\n' "${quality_issues[@]}"
   fi
   
   if [[ ${#quality_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ 品質改善推奨事項:"
       printf '    💡 %s\n' "${quality_warnings[@]}"
   fi
   ```

6. **分析レポートの生成と判定**:
   ```bash
   # 📝 包括的なテスト結果分析レポートの生成
   echo "📝 テスト結果分析レポート作成中..."
   
   analysis_report="docs/reviews/test-results-analysis-${issue_list}-$(date +%Y%m%d).md"
   mkdir -p "$(dirname "$analysis_report")"
   
   # 全体ステータスの判定
   total_issues=$((${#coverage_issues[@]} + ${#scenario_issues[@]} + ${#quality_issues[@]}))
   total_warnings=$((${#coverage_warnings[@]} + ${#scenario_warnings[@]} + ${#quality_warnings[@]}))
   
   if [[ $success_rate -ge 95 && $total_issues -eq 0 && $total_warnings -le 3 ]]; then
       review_status="APPROVED"
       status_icon="✅"
       next_action="リファクタリング推奨"
   elif [[ $success_rate -ge 90 && $total_issues -eq 0 ]]; then
       review_status="CONDITIONAL_APPROVAL"
       status_icon="⚠️"
       next_action="軽微な改善後にリファクタリング可能"
   else
       review_status="REJECTED"
       status_icon="❌"
       next_action="問題修正が必要"
   fi
   
   # 分析レポートの作成
   cat > "$analysis_report" << EOF
# テスト結果分析レポート

**Issues**: $(printf '#%s ' "${issue_numbers[@]}")  
**分析日時**: $(date)  
**ステータス**: $status_icon $review_status  

## テスト実行結果

- **成功率**: ${success_rate}% (${passed_tests}/${total_tests}テスト)
- **実行時間**: ${execution_time}秒
- **失敗テスト**: ${failed_tests}件
- **エラー**: ${error_tests}件

## カバレッジ分析

- **総合カバレッジ**: ${total_coverage:-未特定}%
- **目標達成**: $([ "${total_coverage:-0}" -ge 80 ] && echo "✅" || echo "❌") (target: 80%+)

$(if [[ ${#coverage_issues[@]} -eq 0 ]]; then
    echo "✅ **カバレッジ良好**: 目標を達成しています"
else
    echo "❌ **カバレッジ問題**: 以下の改善が必要です"
    printf '- %s\n' "${coverage_issues[@]}"
fi)

## シナリオ実装評価

$(if [[ ${#scenario_issues[@]} -eq 0 ]]; then
    echo "✅ **シナリオ実装完了**: すべてのGiven-When-Thenシナリオが実装されています"
else
    echo "❌ **シナリオ実装不足**: 以下の改善が必要です"
    printf '- %s\n' "${scenario_issues[@]}"
fi)

## 品質メトリクス評価

$(if [[ ${#quality_issues[@]} -eq 0 ]]; then
    echo "✅ **品質基準達成**: コード品質が基準を満たしています"
else
    echo "❌ **品質問題**: 以下の改善が必要です"
    printf '- %s\n' "${quality_issues[@]}"
fi)

## 推奨アクション

**次のステップ**: $next_action

$(if [[ "$review_status" == "REJECTED" ]]; then
    echo "### 必要な修正"
    echo "1. 批判的問題の修正"
    all_issues=("${coverage_issues[@]}" "${scenario_issues[@]}" "${quality_issues[@]}")
    printf '   - %s\n' "${all_issues[@]}"
    echo "2. テスト結果の改善"
    echo "3. 再分析の実行"
fi)

$(if [[ $total_warnings -gt 0 ]]; then
    echo "### 改善推奨事項"
    all_warnings=("${coverage_warnings[@]}" "${scenario_warnings[@]}" "${quality_warnings[@]}")
    printf '- %s\n' "${all_warnings[@]}"
fi)

## パフォーマンス分析

- **実行時間**: ${execution_time}秒
- **パフォーマンス評価**: $(if [[ $(echo "$execution_time < 5" | bc -l 2>/dev/null || echo "1") -eq 1 ]]; then echo "✅ 良好"; else echo "⚠️ 改善推奨"; fi)

## 次回のアクション項目

- [ ] カバレッジ向上 (target: 85%+)
- [ ] パフォーマンス最適化
- [ ] コード品質改善
- [ ] アーキテクチャ準拠確認
EOF
   
   echo "✅ 分析レポート作成完了: $analysis_report"
   ```

7. **メタデータの更新と判定の提供**:
   ```bash
   # 🔄 テスト分析結果でメタデータを更新
   echo "🔄 メタデータ更新中..."
   
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]]; then
           # メタデータでテスト分析ステータスを更新
           update_metadata_atomic "$metadata_file" \
               --set ".phases.test_results_analysis.analyzed = true" \
               --set ".phases.test_results_analysis.analysis_status = \"$review_status\"" \
               --set ".phases.test_results_analysis.analysis_date = \"$(date -Iseconds)\"" \
               --set ".phases.test_results_analysis.analysis_report = \"$analysis_report\"" \
               --set ".phases.test_results_analysis.test_success_rate = $success_rate" \
               --set ".phases.test_results_analysis.coverage_percentage = ${total_coverage:-0}"
       fi
   done
   
   # 最終判定と次のステップの提供
   echo ""
   echo "$status_icon テスト結果分析結果: $review_status"
   
   case "$review_status" in
       "APPROVED")
           echo "✅ テスト結果分析完了: リファクタリング推奨"
           echo "💡 次のステップ: /refactor ${issue_numbers[*]}"
           ;;
       "CONDITIONAL_APPROVAL")
           echo "⚠️ 条件付き承認: 軽微な改善推奨"
           echo "💡 次のステップ: 改善適用後に /refactor ${issue_numbers[*]}"
           ;;
       "REJECTED")
           echo "❌ 問題修正が必要です"
           echo "💡 次のステップ: 問題修正後に再分析"
           echo ""
           echo "主な修正事項:"
           all_issues=("${coverage_issues[@]}" "${scenario_issues[@]}" "${quality_issues[@]}")
           printf '  - %s\n' "${all_issues[@]}"
           exit 1
           ;;
   esac
   
   echo "🎉 テスト結果レビュー完了!"
   ```

**💡 テスト結果レビューの主要メリット:**
- **品質ゲート検証**: リファクタリング前に実装が品質基準を満たすことを確認
- **パフォーマンス最適化**: ボトルネックと最適化機会の特定  
- **カバレッジギャップ分析**: テストされていないコードパスと重要なシナリオの発見
- **リファクタリングの安全性**: コード改善前の品質ベースラインの確立
- **継続的改善**: 継続的な開発プロセス強化のためのメトリクス収集

**🎯 重要な成功要因:**
- カバレッジギャップに焦点を当てる - これらはしばしば設計や要件の問題を明らかにする
- 本番問題を防ぐためにパフォーマンスボトルネックを早期に特定
- すべてのGiven-When-Thenシナリオが適切に実装されていることを検証
- 品質メトリクスがプロジェクト基準を満たすことを確認
- 分析結果に基づく明確なリファクタリングロードマップの提供