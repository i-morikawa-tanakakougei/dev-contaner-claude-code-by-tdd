ドメイン実装前にTDDテスト設計品質とGiven-When-Thenシナリオカバレッジをレビューする

## メタデータ
- **前提条件**: TDDテストが作成され失敗している（05-create-tests）
- **入力**: Issue番号（必須）、機能名（オプション）
- **出力**: 
  - テスト設計レビューレポート
  - TDD REDフェーズ検証
  - テストレビューステータスで更新された`docs/use_cases/issue-X-Y.json`メタデータ
  - テスト承認または必要な修正
- **依存関係**: テストファイル、ユースケース仕様、ドメイン設計文書
- **実行タイミング**: テスト作成後、ドメイン実装前

## 🎯 **TDD/DDD/レイヤードプロセスコンテキスト**

**🔄 コアワークフロー**: ビジョン(00) → 構造(01) → スプリント(02) → ユースケース(03) → ドメイン(04) → 設計レビュー(04.5) → テスト(05) → **テストレビュー(05.5)** → ドメイン(06) → アプリ(07) → インフラ(08) → UI(09) → テスト(10) → リファクタ(11) → 進化(12) → レビュー(13) → フィードバック(14) → PR(15) → ステータス(16)

**🎨 アーキテクチャ**: クリーンアーキテクチャ（ドメイン→アプリケーション→インフラストラクチャ→プレゼンテーション）  
**🧪 開発**: テスト駆動開発（RED→GREEN→REFACTOR）  
**🏗️ 設計**: ドメイン駆動設計（エンティティ、値オブジェクト、集約、リポジトリ）  
**📋 要件**: 完全なトレーサビリティを持つGiven-When-Thenシナリオ  
**🔄 進化**: /evolve-scenariosコマンドによる継続的シナリオ進化

> 📖 **文書管理システム**: [README.md](./README.md)  
> 🗺️ **現在位置**: スプリント実行フェーズ - テスト設計レビュー（05.5/16）  
> 🎯 **フェーズ目的**: ドメイン実装前のTDDテスト品質検証  
> ⬅️ **前のステージ**: 05-create-tests（TDDテスト作成）  
> ➡️ **次のステージ**: 06-implement-domain（ドメイン層実装）
>
> **📋 3層アーキテクチャ操作**:  
> - 🎯 **戦略的**: `docs/use_cases/core/index.md`（シナリオとの検証）  
> - 📊 **戦術的**: `docs/use_cases/index.md`（テストレビューステータス更新）  
> - 🔧 **実行**: `docs/use_cases/issue-X-Y.json`（テストレビュー結果の追跡）

## 🔍 **TDDテストレビュー: 分析のみ**

**⚠️ 重要な注意:**
- **このステップはテストレビューのみ** - TDDテスト設計品質の分析と評価
- **実装やテスト変更なし** - 評価と検証に集中  
- **品質分析** - テストカバレッジ、シナリオマッピング、REDフェーズ準拠の確認
- **テストレビューレポートのみ生成** - コードやテストの修正なし

**レビュープロセス:**
1. `05-create-tests` ← TDDテスト作成・失敗（REDフェーズ）
2. `05.5-review-test-design` ← **【現在位置】テスト品質レビューと検証**
3. `06-implement-domain` ← ドメイン実装開始（承認の場合）
4. 代替: `05-create-tests`に戻る ← テスト修正（重大な問題が見つかった場合）

**重要なTDD検証焦点:**
- ✅ すべてのテストが適切に失敗（REDフェーズ確認）
- ✅ Given-When-Thenシナリオカバレッジの完全性
- ✅ テスト設計品質と可読性
- ✅ 適切なテスト構造と構成

## よくあるエラーと解決策

### ❌ エラーケース1: テストが通っている（REDフェーズではない）
**原因**: テストが偶発的な実装で作成されたか、既存コードをテストしている  
**解決策**: 
```bash
# テストが適切に失敗することを確認
uv run --frozen pytest tests/ -v
# テストが通る場合、テスト実装を修正
/create-tests <issue-number>  # 適切な失敗テストを再作成
# その後再レビュー
/review-test-design <issue-number>
```

### ❌ エラーケース2: テストファイルが見つからない
**原因**: テスト作成前にテストレビューを試行  
**解決策**: 
```bash
# まずテストを作成
/create-tests <issue-number>
# その後レビューを実行
/review-test-design <issue-number>
```

### ❌ エラーケース3: Given-When-Thenカバレッジが不完全
**原因**: テストが仕様のすべてのシナリオをカバーしていない  
**解決策**: 
- 不足シナリオを分析し対応するテストを追加
- すべての受け入れ基準をカバーするようテスト実装を更新
- ビジネスエッジケースが適切にテストされていることを確保

## 実行例

### ✅ 成功例
```bash
$ /review-test-design 3
🔍 Issues: #3 のテスト設計レビューを開始します
📋 テストファイル分析中...
  📖 分析中: tests/unit/domain/test_trade.py
  📖 分析中: tests/unit/domain/test_enhanced_position.py
  📖 分析中: tests/integration/test_trading_session.py
🔴 TDD REDフェーズ検証中...
  ✅ 全テストが適切に失敗: 15個のテスト
  ✅ ImportError確認: 未実装モジュールによる正常な失敗
📊 Given-When-Thenカバレッジ分析中...
  ✅ シナリオカバレッジ: 100% (5/5シナリオ)
  ✅ 受け入れ基準カバレッジ: 完全
🧪 テスト品質評価中...
  ✅ テスト構造: 適切
  ✅ テスト命名: ビジネス意図が明確
  ✅ アサーション品質: 良好
✅ テスト設計承認: 実装開始可能
🎉 テスト設計レビュー完了!
```

### ❌ 失敗例と修正
```bash
$ /review-test-design 3
🔍 Issues: #3 のテスト設計レビューを開始します
❌ TDD RED フェーズ違反が検出されました:
  ⚠️ 8個のテストが既に成功しています
  ⚠️ Given-When-Thenシナリオ3が未テスト
💡 テスト修正が必要です。以下の改善を推奨:
  1. 成功テストの原因調査と修正
  2. 未カバーシナリオのテスト追加
  3. TDD REDフェーズの再確立

# 修正: テスト問題に対処
$ /create-tests 3  # 失敗テストを修正
$ /review-test-design 3  # 再レビュー
```

## 📋 **TDDテスト設計レビュータスクチェックリスト**

**包括的なテスト設計品質評価にこのチェックリストを使用してください:**

### 🔴 必須タスク

#### **🔴 TDD REDフェーズ検証**
- [ ] **全テストを実行**: `uv run --frozen pytest tests/ -v`を実行してテストステータスを検証
- [ ] **全テストの失敗を確認**: 実装不足によりすべてのテストが失敗することを確認
- [ ] **失敗理由をチェック**: テストが正しい理由で失敗していることを確認（ImportError、NotImplementedError）
- [ ] **偶発的実装なしを検証**: プロダクションコードが偶発的に作成されていないことを確認

#### **📖 Given-When-Thenカバレッジ分析**
- [ ] **テストをシナリオにマッピング**: 各Given-When-Thenシナリオに対応するテストがあることを確認
- [ ] **シナリオ完全性をチェック**: ユースケース仕様のすべてのシナリオがテストされていることを確認
- [ ] **受け入れ基準を検証**: すべての受け入れ基準にテスト検証があることを確認
- [ ] **エッジケースカバレッジをレビュー**: シナリオのエッジケースが適切にテストされていることをチェック

#### **🧪 テスト品質評価**
- [ ] **テスト構造を分析**: ドメイン/アプリケーション/インフラストラクチャ層に従ったテスト構成をレビュー
- [ ] **テスト命名を評価**: テスト名がビジネス意図を明確に表現しているかを評価
- [ ] **テスト可読性をチェック**: テストが理解しやすく保守可能であることを確認
- [ ] **アサーション品質を検証**: テストが実装詳細ではなく振る舞いを検証していることを確認

### 🟡 推奨タスク

#### **🏗️ テストアーキテクチャレビュー**
- [ ] **テスト分離をチェック**: テストが副作用なしに独立して実行されることを確認
- [ ] **モック使用をレビュー**: 外部依存に対するモックの適切な使用を評価
- [ ] **テストデータを検証**: テストフィクスチャとデータ設定の適切性をチェック
- [ ] **テスト保守性を評価**: 要件変更に対してテストが簡単に更新できるかを評価

#### **📊 テストカバレッジ計画**
- [ ] **ユニットテストスコープをレビュー**: ドメインエンティティ、値オブジェクト、サービスがカバーされていることを確認
- [ ] **統合テストスコープをチェック**: リポジトリインターフェースとユースケースがカバーされていることを確認
- [ ] **E2Eテストスコープを検証**: エンドツーエンドシナリオが適切に計画されていることを確認
- [ ] **境界テストを評価**: エラー状況とエッジケースがカバーされていることをチェック

### 🟢 オプションタスク

#### **🔍 高度なテスト設計分析**
- [ ] **パフォーマンステスト考慮事項**: パフォーマンス重要パスのテストが必要かを特定
- [ ] **セキュリティテスト計画**: セキュリティ要件に対応するテストがあるかをチェック
- [ ] **エラーハンドリング検証**: 仕様のエラーシナリオがテストされていることを確認
- [ ] **ビジネスルール適用**: ビジネス不変条件とルールが適切にテストされていることを確認

#### **💰 ドメイン固有テストレビュー（例: 円建て対応）**
- [ ] **通貨計算テスト**: 適切なJPY精度と計算テストを確認
- [ ] **金融精度テスト**: 金額関連計算が必要な精度を維持することを確認
- [ ] **規制準拠テスト**: 規制要件がテストされていることをチェック
- [ ] **ローカライゼーションテストカバレッジ**: 地域ビジネス要件がカバーされていることを確認

#### **🔄 テスト進化評価**
- [ ] **テスト拡張性**: 将来の要件にテストが簡単に対応できるかを評価
- [ ] **リファクタリング安全性**: テストが良いリファクタリング安全網を提供するかを評価
- [ ] **文書価値**: テストが要件の生きた文書として機能するかをチェック
- [ ] **チームコミュニケーション**: テストがチームメンバーにビジネス意図を明確に伝えるかを確認

### **📝 文書化とコミュニケーション**
- [ ] **テストレビューレポートを作成**: 包括的なテスト評価文書を生成
- [ ] **テスト決定を文書化**: 主要なテスト設計選択の根拠を記録
- [ ] **必要な修正をリスト**: 実装前に必要な具体的変更を特定
- [ ] **承認ステータスを生成**: ドメイン実装に対する明確なgo/no-go決定を提供

**💡 プロTip**: シナリオカバレッジに焦点を当てる - これはTDDで最も重要なテストギャップが発生する場所です！

## 📊 **テスト設計品質ゲート**

### **🟢 承認 - ドメイン実装準備完了**
- ✅ すべてのテストが適切に失敗（TDD REDフェーズ確認済み）
- ✅ 完全なGiven-When-Thenシナリオカバレッジ
- ✅ テスト品質がプロジェクト基準を満たしている
- ✅ テスト構造がアーキテクチャパターンに従っている
- ✅ ビジネス意図がテストで明確に表現されている
- ✅ 受け入れ基準が完全にカバーされている

### **🟡 条件付き承認 - 軽微な問題**
- ⚠️ 軽微なテスト品質改善が推奨される
- ⚠️ テスト文書の改善が必要
- ⚠️ オプションのテストカバレッジ改善が提案される
- ✅ コアTDD原則が守られている
- ✅ ブロッキングテスト設計問題が特定されていない

### **🔴 却下 - 大きな問題でテスト再設計必要**
- ❌ テストが通っている（適切なTDD REDフェーズではない）
- ❌ 重要なシナリオカバレッジギャップが特定された
- ❌ テスト品質問題が発見された
- ❌ テストアーキテクチャ問題が検出された
- ❌ ビジネス要件がテストで適切に反映されていない
- 🔄 **アクション**: テスト再設計のため`/create-tests`に戻る

## タスク詳細

1. **安全な環境設定と引数解析**:
   ```bash
   # 🔧 すべての安全操作機能を読み込み
   source "$(dirname "${BASH_SOURCE[0]}")/_setup_safe_environment.sh" "05.5-review-test-design" "$ARGUMENTS"
   
   # テストレビューは少なくとも1つのイシュー番号を期待
   if [[ ${#issue_numbers[@]} -eq 0 ]]; then
       echo "エラー: 少なくとも1つのイシュー番号を指定してください"
       show_usage_example "review-test-design" "3" "単一イシューのテスト設計レビュー"
       show_usage_example "review-test-design" "3,7" "複数イシューのテスト設計レビュー"
       show_usage_example "review-test-design" "3 trading-execution" "イシュー + 機能名指定"
       exit 1
   fi
   
   # イシューリストと機能名を抽出
   issue_list=$(IFS=-; echo "${issue_numbers[*]}")
   if [[ ${#other_args[@]} -gt 0 ]]; then
       feature_name="${other_args[0]}"
   else
       # 機能名は既存テストファイルから抽出される
       feature_name=""
   fi
   
   echo "🔍 Issues: $(printf '#%s ' "${issue_numbers[@]}")のテスト設計レビューを開始します"
   ```

2. **前提条件の検証とテストファイルの特定**:
   ```bash
   # 📋 テストファイルが存在することを検証
   echo "📋 テストファイル分析中..."
   
   missing_tests=()
   test_files=()
   
   # イシューに関連するテストファイルを検索
   for issue_num in "${issue_numbers[@]}"; do
       # 標準的な場所でテストファイルを探す
       found_test_files=$(find tests/ -name "*test*" -type f -name "*.py" 2>/dev/null | head -10)
       
       if [[ -z "$found_test_files" ]]; then
           missing_tests+=("Issue #$issue_num")
       else
           while IFS= read -r test_file; do
               [[ -n "$test_file" ]] && test_files+=("$test_file")
           done <<< "$found_test_files"
       fi
   done
   
   # 不足テストをチェック
   if [[ ${#test_files[@]} -eq 0 ]]; then
       echo "❌ テストファイルが見つかりません"
       echo "💡 最初に以下のコマンドでテストを作成してください:"
       echo "   /create-tests ${issue_numbers[*]}"
       exit 1
   fi
   
   # 重複を削除し見つかったテストを表示
   unique_test_files=($(printf '%s\n' "${test_files[@]}" | sort -u))
   for test_file in "${unique_test_files[@]:0:5}"; do
       echo "  📖 分析中: $test_file"
   done
   [[ ${#unique_test_files[@]} -gt 5 ]] && echo "  📖 ... 他 $((${#unique_test_files[@]} - 5)) ファイル"
   
   echo "✅ テストファイル確認完了: ${#unique_test_files[@]} 個のテストファイルを発見"
   ```

3. **TDD REDフェーズ検証**:
   ```bash
   # 🔴 TDD REDフェーズ準拠を検証
   echo "🔴 TDD REDフェーズ検証中..."
   
   # テストを実行して適切に失敗するかチェック
   test_results=$(uv run --frozen pytest tests/ -v --tb=short 2>&1 || echo "")
   
   # 成功・失敗テスト数をカウント
   passed_count=$(echo "$test_results" | grep -c "PASSED" || echo "0")
   failed_count=$(echo "$test_results" | grep -c "FAILED\|ERROR" || echo "0")
   total_tests=$((passed_count + failed_count))
   
   red_phase_violations=()
   red_phase_warnings=()
   
   if [[ $passed_count -gt 0 ]]; then
       red_phase_violations+=("${passed_count}個のテストが既に成功しています (TDD REDフェーズ違反)")
   fi
   
   if [[ $failed_count -eq 0 ]]; then
       red_phase_violations+=("失敗するテストが見つかりません")
   fi
   
   # 失敗理由をチェック
   if echo "$test_results" | grep -q "ImportError\|ModuleNotFoundError"; then
       echo "  ✅ ImportError確認: 未実装モジュールによる正常な失敗"
   elif echo "$test_results" | grep -q "NotImplementedError"; then
       echo "  ✅ NotImplementedError確認: 未実装メソッドによる正常な失敗"
   else
       red_phase_warnings+=("失敗理由が不明確です - 実装が存在している可能性があります")
   fi
   
   # TDD REDフェーズステータスをレポート
   if [[ ${#red_phase_violations[@]} -eq 0 ]]; then
       echo "  ✅ 全テストが適切に失敗: ${failed_count}個のテスト"
       echo "  ✅ TDD REDフェーズ確認済み"
   else
       echo "  ❌ TDD REDフェーズ違反が検出されました:"
       printf '    ⚠️ %s\n' "${red_phase_violations[@]}"
   fi
   
   if [[ ${#red_phase_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ TDD警告事項:"
       printf '    💡 %s\n' "${red_phase_warnings[@]}"
   fi
   ```

4. **Given-When-Thenカバレッジ分析**:
   ```bash
   # 📊 Given-When-Thenシナリオカバレッジを分析
   echo "📊 Given-When-Thenカバレッジ分析中..."
   
   coverage_issues=()
   coverage_warnings=()
   
   # ユースケース仕様ファイルを検索
   spec_files=()
   for issue_num in "${issue_numbers[@]}"; do
       spec_file=$(find docs/use_cases/ -name "*issue-*${issue_num}*" -type f -name "*.md" | head -1)
       if [[ -n "$spec_file" ]]; then
           spec_files+=("$spec_file")
       fi
   done
   
   if [[ ${#spec_files[@]} -gt 0 ]]; then
       # 仕様からシナリオを抽出
       total_scenarios=0
       covered_scenarios=0
       
       for spec_file in "${spec_files[@]}"; do
           # Given-When-Thenシナリオをカウント
           scenarios=$(grep -c "Given:\|When:\|Then:" "$spec_file" 2>/dev/null || echo "0")
           total_scenarios=$((total_scenarios + scenarios / 3))  # 各シナリオが3つの部分を持つと仮定
           
           # シナリオがテストでカバーされているかチェック
           scenario_patterns=$(grep -E "Given:|When:|Then:" "$spec_file" 2>/dev/null | head -15 || echo "")
           if [[ -n "$scenario_patterns" ]]; then
               while IFS= read -r pattern; do
                   # 関連するテストメソッドを探す
                   test_pattern=$(echo "$pattern" | sed 's/Given:\|When:\|Then://g' | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/_/g')
                   if find tests/ -name "*.py" -exec grep -l "$test_pattern\|test.*$(echo "$test_pattern" | cut -c1-10)" {} \; 2>/dev/null | head -1 >/dev/null; then
                       covered_scenarios=$((covered_scenarios + 1))
                   fi
               done <<< "$scenario_patterns"
           fi
       done
       
       # カバレッジ割合を計算
       if [[ $total_scenarios -gt 0 ]]; then
           coverage_percent=$(( (covered_scenarios * 100) / total_scenarios ))
           echo "  📊 シナリオカバレッジ: ${coverage_percent}% (${covered_scenarios}/${total_scenarios}シナリオ)"
           
           if [[ $coverage_percent -ge 90 ]]; then
               echo "  ✅ 受け入れ基準カバレッジ: 良好"
           elif [[ $coverage_percent -ge 70 ]]; then
               coverage_warnings+=("シナリオカバレッジが不十分: ${coverage_percent}%")
           else
               coverage_issues+=("シナリオカバレッジが低すぎます: ${coverage_percent}%")
           fi
       else
           coverage_warnings+=("Given-When-Thenシナリオが仕様書で見つかりません")
       fi
   else
       coverage_warnings+=("ユースケース仕様ファイルが見つかりません")
   fi
   
   # カバレッジステータスをレポート
   if [[ ${#coverage_issues[@]} -eq 0 ]]; then
       echo "  ✅ Given-When-Thenカバレッジ: 適切"
   else
       echo "  ❌ カバレッジ問題が検出されました:"
       printf '    ⚠️ %s\n' "${coverage_issues[@]}"
   fi
   
   if [[ ${#coverage_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ カバレッジ改善推奨事項:"
       printf '    💡 %s\n' "${coverage_warnings[@]}"
   fi
   ```

5. **テスト品質評価**:
   ```bash
   # 🧪 テスト設計品質を評価
   echo "🧪 テスト品質評価中..."
   
   quality_issues=()
   quality_warnings=()
   
   # テスト構造を分析
   unit_tests=$(find tests/ -path "*/unit/*" -name "*.py" | wc -l)
   integration_tests=$(find tests/ -path "*/integration/*" -name "*.py" | wc -l)
   e2e_tests=$(find tests/ -path "*/e2e/*" -name "*.py" | wc -l)
   
   echo "  📊 テスト構造分析:"
   echo "    - ユニットテスト: ${unit_tests}ファイル"
   echo "    - 統合テスト: ${integration_tests}ファイル"
   echo "    - E2Eテスト: ${e2e_tests}ファイル"
   
   # テスト命名規則をチェック
   poorly_named_tests=0
   well_named_tests=0
   
   for test_file in "${unique_test_files[@]}"; do
       # テストメソッドをカウント
       test_methods=$(grep -c "def test_" "$test_file" 2>/dev/null || echo "0")
       
       # 説明的なテスト名をチェック
       descriptive_names=$(grep "def test_" "$test_file" 2>/dev/null | grep -c "_when_\|_should_\|_given_\|_then_" || echo "0")
       
       if [[ $descriptive_names -gt 0 ]] && [[ $test_methods -gt 0 ]]; then
           if [[ $((descriptive_names * 100 / test_methods)) -ge 70 ]]; then
               well_named_tests=$((well_named_tests + descriptive_names))
           else
               poorly_named_tests=$((poorly_named_tests + test_methods - descriptive_names))
           fi
       else
           poorly_named_tests=$((poorly_named_tests + test_methods))
       fi
   done
   
   # 品質評価をレポート
   if [[ $unit_tests -gt 0 ]] && [[ $integration_tests -gt 0 ]]; then
       echo "  ✅ テスト構造: 適切"
   else
       quality_warnings+=("テスト構造の改善推奨: ユニット/統合テストの分離")
   fi
   
   if [[ $well_named_tests -gt $poorly_named_tests ]]; then
       echo "  ✅ テスト命名: ビジネス意図が明確"
   else
       quality_warnings+=("テスト命名の改善推奨: ビジネス意図を明確に")
   fi
   
   # シンプルなアサーション品質チェック
   assertion_count=0
   for test_file in "${unique_test_files[@]}"; do
       assertions=$(grep -c "assert\|assertEqual\|assertTrue\|assertFalse" "$test_file" 2>/dev/null || echo "0")
       assertion_count=$((assertion_count + assertions))
   done
   
   if [[ $assertion_count -gt 0 ]]; then
       echo "  ✅ アサーション品質: 良好"
   else
       quality_issues+=("アサーションが見つかりません")
   fi
   
   # 品質ステータスをレポート
   if [[ ${#quality_issues[@]} -eq 0 ]]; then
       echo "  ✅ テスト品質: 基準を満たしています"
   else
       echo "  ❌ テスト品質問題が検出されました:"
       printf '    ⚠️ %s\n' "${quality_issues[@]}"
   fi
   
   if [[ ${#quality_warnings[@]} -gt 0 ]]; then
       echo "  ⚠️ テスト品質改善推奨事項:"
       printf '    💡 %s\n' "${quality_warnings[@]}"
   fi
   ```

6. **レビューレポートと決定の生成**:
   ```bash
   # 📝 包括的テストレビューレポートを生成
   echo "📝 テスト設計レビューレポート作成中..."
   
   review_report="docs/reviews/test-design-review-${issue_list}-$(date +%Y%m%d).md"
   mkdir -p "$(dirname "$review_report")"
   
   # 全体ステータスを決定
   if [[ ${#red_phase_violations[@]} -eq 0 && ${#coverage_issues[@]} -eq 0 && ${#quality_issues[@]} -eq 0 ]]; then
       review_status="APPROVED"
       status_icon="✅"
       next_action="実装開始可能"
   elif [[ ${#red_phase_violations[@]} -eq 0 && ${#coverage_issues[@]} -eq 0 ]]; then
       review_status="CONDITIONAL_APPROVAL"
       status_icon="⚠️"
       next_action="軽微な改善後に実装可能"
   else
       review_status="REJECTED"
       status_icon="❌"
       next_action="テスト修正が必要"
   fi
   
   # レビューレポートを作成
   cat > "$review_report" << EOF
# テスト設計レビューレポート

**Issues**: $(printf '#%s ' "${issue_numbers[@]}")  
**レビュー日時**: $(date)  
**ステータス**: $status_icon $review_status  

## テスト分析結果

- **テストファイル数**: ${#unique_test_files[@]}個
- **実行テスト数**: ${total_tests}個
- **失敗テスト数**: ${failed_count}個 (TDD RED: $([ $failed_count -eq $total_tests ] && echo "✅" || echo "❌"))

## TDD REDフェーズ評価

$(if [[ ${#red_phase_violations[@]} -eq 0 ]]; then
    echo "✅ **TDD準拠**: 全テストが適切に失敗しています"
else
    echo "❌ **TDD違反検出**: 以下の問題が発見されました"
    printf '- %s\n' "${red_phase_violations[@]}"
fi)

## Given-When-Thenカバレッジ評価

$(if [[ ${#coverage_issues[@]} -eq 0 ]]; then
    echo "✅ **カバレッジ良好**: シナリオカバレッジが適切です"
else
    echo "⚠️ **カバレッジ課題**: 以下の改善が必要です"
    printf '- %s\n' "${coverage_issues[@]}"
fi)

## テスト品質評価

$(if [[ ${#quality_issues[@]} -eq 0 ]]; then
    echo "✅ **品質良好**: テスト設計が基準を満たしています"
else
    echo "⚠️ **品質課題**: 以下の改善が推奨されます"
    printf '- %s\n' "${quality_issues[@]}"
fi)

## 推奨アクション

**次のステップ**: $next_action

$(if [[ "$review_status" == "REJECTED" ]]; then
    echo "### 必要な修正"
    echo "1. TDD REDフェーズの確立"
    printf '   - %s\n' "${red_phase_violations[@]}"
    echo "2. シナリオカバレッジの改善"
    printf '   - %s\n' "${coverage_issues[@]}"
    echo "3. テストの再実行とレビュー"
fi)

$(if [[ ${#coverage_warnings[@]} -gt 0 || ${#quality_warnings[@]} -gt 0 ]]; then
    echo "### 改善推奨事項"
    printf '- %s\n' "${coverage_warnings[@]}" "${quality_warnings[@]}"
fi)
EOF
   
   echo "✅ レビューレポート作成完了: $review_report"
   ```

7. **メタデータの更新と決定の提供**:
   ```bash
   # 🔄 テストレビュー結果でメタデータを更新
   echo "🔄 メタデータ更新中..."
   
   for issue_num in "${issue_numbers[@]}"; do
       metadata_file=$(find docs/use_cases -name "issue-${issue_num}-*.json" | head -1)
       if [[ -f "$metadata_file" ]]; then
           # メタデータでテストレビューステータスを更新
           update_metadata_atomic "$metadata_file" \
               --set ".phases.test_design_review.reviewed = true" \
               --set ".phases.test_design_review.review_status = \"$review_status\"" \
               --set ".phases.test_design_review.review_date = \"$(date -Iseconds)\"" \
               --set ".phases.test_design_review.review_report = \"$review_report\"" \
               --set ".phases.test_design_review.test_count = $total_tests" \
               --set ".phases.test_design_review.failing_tests = $failed_count"
       fi
   done
   
   # 最終決定と次のステップを提供
   echo ""
   echo "$status_icon テスト設計レビュー結果: $review_status"
   
   case "$review_status" in
       "APPROVED")
           echo "✅ テスト設計承認: 実装開始可能"
           echo "💡 次のステップ: /implement-domain ${issue_numbers[*]}"
           ;;
       "CONDITIONAL_APPROVAL")
           echo "⚠️ 条件付き承認: 軽微な改善推奨"
           echo "💡 次のステップ: 改善適用後に /implement-domain ${issue_numbers[*]}"
           ;;
       "REJECTED")
           echo "❌ テスト修正が必要です"
           echo "💡 次のステップ: /create-tests ${issue_numbers[*]} (テスト再作成)"
           echo ""
           echo "主な修正事項:"
           printf '  - %s\n' "${red_phase_violations[@]}" "${coverage_issues[@]}" "${quality_issues[@]}"
           exit 1
           ;;
   esac
   
   echo "🎉 テスト設計レビュー完了!"
   ```

**💡 テスト設計レビューの主要メリット:**
- **TDD準拠**: 実装前の適切なREDフェーズを確保
- **シナリオカバレッジ**: すべてのGiven-When-Thenシナリオがテストされていることを検証  
- **テスト品質**: テスト設計品質と保守性を評価
- **コスト削減**: 高コストな実装開始前にテスト問題を修正
- **チーム整合**: テストがビジネス要件を明確に伝達することを確保

**🎯 重要な成功要因:**
- TDD REDフェーズ検証に焦点を当てる - 適切なTDDサイクルで最も重要
- 完全なGiven-When-Thenシナリオカバレッジを検証
- テストがビジネス意図を明確に保守可能に表現していることを確認
- テストが正しい理由（実装不足）で失敗していることをチェック
- テスト構造がアーキテクチャパターンに従っていることを確認